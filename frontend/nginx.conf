# Раздача SPA + проксирование API на бэкенд (имя сервиса из docker-compose).
server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Держим синхронным с upload_max_size_mb в backend/app/config.py — иначе
    # nginx обрывает запрос дефолтным лимитом 1m раньше, чем он дойдёт до
    # FastAPI, и клиент получает невнятную ошибку без detail в теле ответа.
    client_max_body_size 5m;

    # Vue Router работает в history-режиме — все пути отдают index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Бэкенд сам живёт под /api — путь передаётся как есть
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
