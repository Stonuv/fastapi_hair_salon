% main.tex -- содержание документа. Оформление и пакеты -- в config.tex,
% титульный лист -- в titlepage.tex.
% Пояснительная записка -- по образцу example.pdf (структура и оформление
% ГОСТ-документа: РЕФЕРАТ, список сокращений, разделы \gostsection), но с
% содержанием по structure.md, а не полным перечнем вопросов из example.pdf
% (см. также final_report/main.tex -- тот же проект, содержание почти
% дословно то же, другая рамка документа и оформление).
\input{config}

\begin{document}

% ============================================================
% ТИТУЛЬНЫЙ ЛИСТ -- отдельный файл titlepage.tex (бланк Tit_list_PP.pdf)
% ============================================================
\input{titlepage}
\setcounter{page}{2}

% ============================================================
\renewcommand{\contentsname}{СОДЕРЖАНИЕ}
\begingroup
\titleformat{\section}{\bfseries\centering}{\thesection}{0.5em}{}
\tableofcontents
\endgroup
\newpage
% ============================================================

% ============================================================
% Списки сокращений и терминов -- в основной части, сразу после содержания,
% двумя отдельными списками: сначала сокращения, затем термины (п. 5.7
% методического пособия). \section* без \addcontentsline: в содержании эти
% списки не указываются.
% ============================================================
\section*{\hspace*{\gostindent}СПИСОК СОКРАЩЕНИЙ}

\begin{itemize}[leftmargin=0pt, itemindent=1.5cm, labelsep=0pt, label={}]
\item \textbf{API} -- Application Programming Interface -- программный интерфейс
  взаимодействия компонентов системы.
\item \textbf{CI} -- Continuous Integration -- непрерывная автоматическая проверка
  (тесты, линтеры) при каждом изменении кода.
\item \textbf{CMS} -- Content Management System -- система управления содержимым сайта.
\item \textbf{CRM} -- Customer Relationship Management -- управление
  взаимоотношениями с клиентами.
\item \textbf{CSP} -- Content Security Policy -- политика безопасности контента
  браузера.
\item \textbf{DTO} -- Data Transfer Object -- объект переноса данных на границе API,
  отделённый от внутреннего представления в БД.
\item \textbf{HTTP} -- HyperText Transfer Protocol -- протокол передачи данных
  прикладного уровня; \textbf{HTTPS} -- тот же протокол поверх шифрованного
  канала TLS.
\item \textbf{JSON} -- JavaScript Object Notation -- текстовый формат обмена
  данными.
\item \textbf{JWT} -- JSON Web Token -- формат токена авторизации, используемый для
  access-токена сессии.
\item \textbf{KPI} -- Key Performance Indicator -- ключевой показатель
  эффективности.
\item \textbf{MVCC} -- Multiversion Concurrency Control -- многоверсионное
  управление конкурентным доступом в СУБД.
\item \textbf{OAuth} -- открытый протокол делегированной авторизации через
  внешнего поставщика учётных записей.
\item \textbf{ORM} -- Object-Relational Mapping -- отображение объектов языка
  программирования на строки реляционной таблицы.
\item \textbf{REST} -- Representational State Transfer -- архитектурный стиль
  построения API поверх HTTP.
\item \textbf{SEO} -- Search Engine Optimization -- оптимизация страниц под
  поисковые системы.
\item \textbf{SPA} -- Single Page Application -- одностраничное веб-приложение
  (фронтенд).
\item \textbf{SQL} -- Structured Query Language -- язык запросов к реляционной
  СУБД.
\item \textbf{SSR} -- Server-Side Rendering -- формирование страницы на стороне
  сервера.
\item \textbf{TLS} -- Transport Layer Security -- протокол шифрования канала связи
  (HTTPS).
\item \textbf{UI} -- User Interface -- пользовательский интерфейс.
\item \textbf{VK ID} -- сервис единой учётной записи «ВКонтакте», используемый как
  внешний поставщик авторизации.
\item \textbf{БД} -- база данных.
\item \textbf{СУБД} -- система управления базами данных (в проекте -- PostgreSQL 16).
\item \textbf{ТЗ} -- техническое задание.
\item \textbf{ФТ} -- функциональное требование (карточки ФТ-001--ФТ-016 в
  техническом задании).
\end{itemize}

\section*{\hspace*{\gostindent}СПИСОК ТЕРМИНОВ}

\begin{itemize}[leftmargin=0pt, itemindent=1.5cm, labelsep=0pt, label={}]
\item \textbf{Мастер} -- сотрудник точки, оказывающий услуги клиентам; в системе --
  роль пользователя.
\item \textbf{Слот} -- свободный интервал в расписании мастера, доступный для
  записи клиента.
\item \textbf{Точка сети} -- отдельная парикмахерская или барбершоп в составе сети
  одного владельца -- собственный адрес, часы работы, мастера и записи.
\end{itemize}

% ============================================================
\section*{\hspace*{\gostindent}ВВЕДЕНИЕ}
\addcontentsline{toc}{section}{ВВЕДЕНИЕ}
% ============================================================

Предприятия сферы парикмахерских услуг и барберинга всё чаще переходят
от бумажных журналов и разрозненных таблиц к специализированным
программным системам. Такие решения обеспечивают онлайн-запись
клиентов, ведение расписания мастеров и автоматизацию отчётности.
Отсутствие единого инструмента порождает типовые для отрасли проблемы:
возникновение накладок в расписании (двойных записей), неявки клиентов
из-за отсутствия напоминаний, сложности с дифференцированным учётом цен
мастеров, а также отсутствие у руководства оперативной аналитики по
выручке и загрузке заведений. Настоящий документ посвящён
веб-приложению «Сайтама», автоматизирующему данные бизнес-процессы для
отдельных салонов и сетевых барбершопов.

В работе формулируется решаемая задача, описываются автоматизируемые
процессы, архитектура приложения, применённые паттерны проектирования и
требования нормативно-правовой базы. Обосновывается выбор
технологического стека (FastAPI [1], SQLAlchemy [2], PostgreSQL [3]),
приводится схема базы данных, детально рассматривается логика ключевых
пользовательских сценариев, интеграция аналитических модулей и
развёртывание сервиса в сети Интернет.

Комплект проектной документации включает три сопутствующих документа:

\begin{enumerate}
\item Техническое задание, оформленное в соответствии с
  ГОСТ 34.602-2020 [4] и ГОСТ 19.201-78 [5], содержащее постановку
  задачи и перечень функциональных требований (ФТ-001--ФТ-016);
\item Руководство пользователя, описывающее пошаговые сценарии работы
  для четырёх ролей: клиента, мастера, администратора филиала и
  владельца сети;
\item Руководство администратора, регламентирующее процедуры
  развёртывания, эксплуатации и резервного копирования серверной
  инфраструктуры.
\end{enumerate}

Практическая значимость результатов заключается в создании готовой к
практическому применению программной платформы, устраняющей издержки
ручного учёта и человеческого фактора. Архитектура системы изначально
поддерживает мультифилиальность, что позволяет развёртывать её как в
единичных парикмахерских, так и в разветвлённых сетях без внесения
изменений в исходный код.

% ============================================================
\gostsection{1}{Постановка задачи}
% ============================================================

\gostsubsection{1.1}{Цель и задачи разработки}

Большинство предприятий малого бизнеса в сфере парикмахерских услуг и
барберинга ведут запись клиентов и операционный учёт вручную -- посредством
телефонных звонков, бумажных журналов и разрозненных электронных таблиц.
Подобный подход порождает типовые для отрасли проблемы: возникновение
накладок в расписании (двойных бронирований), высокий процент неявок
клиентов (no-show) из-за отсутствия автоматических напоминаний, сложности
с дифференцированным расчётом стоимости услуг по мастерам, а также
невозможность оперативного получения сводной аналитики по выручке и
загрузке заведений без трудоёмкого ручного подсчёта.

Основной целью работы является проектирование и разработка веб-приложения
«Сайтама», автоматизирующего полный цикл операционной деятельности
парикмахерской или барбершопа: организацию самостоятельной онлайн-записи
клиентов без участия администратора, ведение каталога услуг с
индивидуальным ценообразованием, отправку автоматических напоминаний о
предстоящих визитах, а также генерацию отчётности по ключевым
бизнес-показателям. Архитектура разрабатываемой системы изначально
предусматривает поддержку мультифилиальности с разграничением прав
доступа между администратором конкретного филиала и владельцем всей сети
заведений.

Достижение поставленной цели оценивается следующими измеримыми
показателями (детализированными в 2.2 технического задания):

\begin{itemize}
  \item сокращение среднего времени оформления записи со стороны клиента
    до 2 минут;
  \item полное исключение вероятности двойных записей программно-техническими
    средствами за счёт ограничений целостности СУБД, а не административных
    регламентов;
  \item снижение доли неявок клиентов за счёт внедрения автоматических
    уведомлений;
  \item реализация гибкой настройки прайс-листа с учётом квалификации
    конкретного мастера;
  \item консолидация данных в едином реляционном хранилище и отказ от
    бумажного документооборота;
  \item обеспечение возможности самостоятельного формирования отчётов по
    выручке и загруженности заведений конечными пользователями без
    привлечения технических специалистов.
\end{itemize}

Границы проекта строго зафиксированы: за рамками разработки оставлены
модули интернет-эквайринга (расчёты производятся очно на месте оказания
услуг согласно 3.2 технического задания), ведение складского и бухгалтерского
учёта, а также расширенные маркетинговые инструменты CRM-системы
(сегментация клиентской базы, массовые рассылки и программы лояльности).

\gostsubsection{1.2}{Автоматизируемые бизнес-процессы}

Система автоматизирует следующие шесть ключевых бизнес-процессов, выделенных
при обследовании предметной области (техническое задание, 2.1):

\begin{enumerate}
  \item \textbf{Онлайн-запись клиентов (Зап-1)} -- выбор филиала, услуги,
        мастера и свободного временного слота с последующим
        подтверждением записи без участия администратора. Для новых
        клиентов процедура бронирования предусматривает автоматическое
        создание учётной записи (раздел 5).
  \item \textbf{Управление каталогом услуг и ценообразованием (Усл-2)} --
        ведение номенклатуры услуг с возможностью гибкой настройки
        стоимости для каждой связки «мастер -- услуга».
  \item \textbf{Оповещение и напоминания о визитах (Увед-3)} --
        автоматическая отправка уведомлений клиентам за 24 и за 2 часа до
        назначенного времени приёма.
  \item \textbf{Формирование аналитической отчётности (Отчёт-4)} --
        расчёт показателей выручки, загруженности мастеров и рейтинга
        востребованности услуг за произвольный период в пользовательском
        интерфейсе без необходимости прямого обращения к базе данных
        (5.4).
  \item \textbf{Управление контентом витрины (Конт-5)} -- администрирование
        информационной составляющей публичной части сервиса (главная
        страница, сведения о мастерах и их портфолио).
  \item \textbf{Управление филиальной сетью (Точ-6)} -- создание,
        редактирование и вывод из эксплуатации точек обслуживания с
        настройкой их адресов, контактных данных, графиков работы и
        разграничением прав доступа между локальными администраторами и
        владельцем сети (руководство пользователя, подразделы
        3.4.1--3.4.4).
\end{enumerate}

% ============================================================
\gostsection{2}{Архитектура программного обеспечения}
% ============================================================

\gostsubsection{2.1}{Общая схема}

Разрабатываемая система представляет собой клиент-серверное
веб-приложение, состоящее из одностраничного клиентского интерфейса (SPA)
на базе фреймворка Vue 3 [6] и серверной части с REST API на FastAPI
[1], взаимодействующей с централизованной реляционной СУБД PostgreSQL
[3].

Слоистая архитектура бэкенда и схема его взаимодействия с клиентским
приложением представлены в соответствии с рисунком~1. Топология развёртывания
компонентов системы (контейнеризация на базе Docker, конфигурация
обратного прокси-сервера и настройка протокола TLS) детально приведена в
руководстве администратора (раздел 1).

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}[
  layer/.style={draw, rectangle, text width=9.5cm, minimum height=1.7cm, align=center, font=\small},
  note/.style={font=\small\itshape, align=left, text width=5.6cm},
  conn/.style={draw},
]
\node[layer] (vue) at (0,10) {Vue 3 SPA: views / components / stores (Pinia) / composables};
\node[layer] (api) at (0,8) {REST API (JSON поверх HTTPS)};
\node[layer] (routes) at (0,6) {routes/ -- APIRouter по ресурсам; Pydantic-схемы на границе (DTO)};
\node[layer] (services) at (0,4) {services/ -- бизнес-правила, проверка ролей};
\node[layer] (repos) at (0,2) {repositories/ -- один класс на агрегат, SQLAlchemy 2.0 select()};
\node[layer] (db) at (0,0) {PostgreSQL 16 (models/, миграции Alembic)};
\draw[conn] (vue) -- (api);
\draw[conn] (api) -- (routes);
\draw[conn] (routes) -- (services);
\draw[conn] (services) -- (repos);
\draw[conn] (repos) -- (db);
\node[note, right=0.4cm of routes] {get\_db() -- сессия на запрос, unit-of-work: commit при успехе, rollback при исключении};
\node[note, right=0.4cm of services] {require\_role(...) -- фабрика зависимостей: owner $\supset$ admin};
\node[note, right=0.4cm of repos] {без общей абстрактной репозитория-базы; общая логика (пагинация, экранирование LIKE) -- свободными функциями};
\end{tikzpicture}%
}
\caption{Слоистая архитектура: от фронтенда до базы данных}
\end{figure}

\gostsubsection{2.2}{Архитектурные шаблоны бэкенда}

Серверная часть приложения (\envvar{backend/app/}) реализована на основе
строгой однонаправленной слоистой архитектуры, включающей уровни
маршрутизации (\texttt{routes}), бизнес-логики (\texttt{services}),
доступа к данным (\texttt{repositories}) и объектно-реляционных моделей
(\texttt{models}).

Вспомогательный пакет \texttt{schemas/}, содержащий Pydantic-схемы [7],
применяется исключительно на границе обработки входящих и исходящих
HTTP-запросов. Сервисный слой выполняет валидацию и сериализацию данных
через вызов \envvar{XResponse.model\_validate(orm\_obj)}, что исключает
прямую передачу внутренних ORM-сущностей во внешнее API.

В архитектуре серверной части применены следующие паттерны и подходы к
проектированию:

\begin{itemize}
  \item \textbf{Объект переноса данных (Data Transfer Object, DTO)} --
        разделение контрактов API (\texttt{schemas/}) и структур хранения
        базы данных (\texttt{models/}) обеспечивает независимость
        публичного интерфейса от изменений внутренней схемы данных;
  \item \textbf{Репозиторий (Repository)} -- инкапсуляция логики
        взаимодействия с СУБД через SQLAlchemy [2] в специализированных
        классах (\texttt{UserRepository}, \texttt{AppointmentRepository},
        \texttt{SalonRepository}). Слой бизнес-логики изолирован от
        формирования прямых SQL-запросов;
  \item \textbf{Внедрение зависимостей (Dependency Injection) и единица
        работы (Unit of Work)} -- передача зависимостей осуществляется
        через встроенный механизм \texttt{Depends} фреймворка FastAPI.
        Генератор сессий \texttt{get\_db()} реализует паттерн Unit of
        Work: в рамках одного HTTP-запроса выполняется единая транзакция
        с автоматической фиксацией изменений (\texttt{commit}) при
        успешном ответе, откатом (\texttt{rollback}) при возникновении
        исключений и гарантированным закрытием сессии (\texttt{close}) в
        блоке \texttt{finally};
  \item \textbf{Фабрика зависимостей (Dependency Factory)} --
        параметризованная функция контроля доступа
        \envvar{require\_role(*roles)} генерирует функции-зависимости для
        аутентификации пользователей (\envvar{get\_current\_client},
        \envvar{get\_current\_master}, \envvar{get\_current\_admin},
        \envvar{get\_current\_owner}). Поскольку права роли \texttt{owner}
        включают в себя все привилегии роли \texttt{admin}, проверка
        \texttt{get\_current\_admin} предоставляет доступ обеим категориям
        пользователей;
  \item \textbf{Контроль контекста филиала (Salon Scoping)} -- функция
        \envvar{resolve\_salon\_scope(current\_user, salon\_id)}
        осуществляет явное разграничение области видимости данных внутри
        защищённых эндпоинтов. Для роли \texttt{owner} отсутствие
        параметра \texttt{salon\_id} означает доступ ко всей сети
        филиалов, тогда как для роли \texttt{admin} доступ строго
        ограничен закреплённым за ним филиалом (при попытке
        несанкционированного доступа возвращается HTTP-код 403).
\end{itemize}

\gostsubsection{2.3}{Архитектура фронтенда}

Клиентская часть приложения (\envvar{frontend/src/}) построена на базе
фреймворка Vue 3 с применением Composition API (с повсеместным
использованием синтаксиса \texttt{<script setup>} на базе JavaScript),
маршрутизатора Vue Router 4 и библиотеки управления состоянием Pinia.
Централизованное состояние разделено на шесть изолированных хранилищ
(Setup Stores): \texttt{auth}, \texttt{salon}, \texttt{setup},
\texttt{siteContent}, \texttt{masterProfile} и \texttt{toast}.

Маршрутизация реализована в модуле \envvar{router/index.js} с применением
динамического импорта (ленивой загрузки) компонентов страниц. Глобальный
навигационный хук \texttt{beforeEach} выполняет ролевую модель
разграничения доступа на основе метаполей маршрутов (\texttt{meta.roles}),
а также перенаправляет пользователей на интерфейс мастера первоначальной
настройки системы (\texttt{/setup}) до завершения базовой конфигурации
сервиса (5.3).

В архитектуре фронтенда реализованы два ключевых решения, обеспечивающих
безопасность и гибкость клиентской части:

\begin{itemize}
  \item \textbf{Безопасное хранение и обновление токенов аутентификации}
        -- токен сессии передаётся исключительно через cookie-файлы с
        флагом HttpOnly, исключая его хранение в \envvar{localStorage} или
        переменных JavaScript, что минимизирует риски компрометации через
        XSS-атаки. Обновление пары токенов реализовано через перехватчик
        (interceptor) библиотеки Axios по паттерну Single-Flight: при
        получении ответа с кодом 401 инициируется единственный запрос к
        эндпоинту \envvar{POST /auth/refresh}, в то время как параллельные
        асинхронные вызовы ожидают разрешения единого промиса (Promise).
        Полный сброс сессии и перенаправление на форму входа (logout)
        выполняются только в случае неуспешного ответа самого запроса на
        обновление токена.
  \item \textbf{Динамическая тема оформления} -- корпоративная цветовая
        палитра (\envvar{brand}, \envvar{accent}, \envvar{ink},
        \envvar{stone}) объявлена в конфигурации Tailwind CSS через
        пользовательские CSS-переменные (CSS Custom Properties). Значения
        переменных динамически считываются из конфигурации сайта в
        runtime, что позволяет адаптировать визуальный стиль под бренд
        конкретного филиала без повторной компиляции исходного кода
        фронтенда.
\end{itemize}

\gostsubsection{2.4}{Требования законодательства и защита данных}

В соответствии с техническим заданием (1.4), система
спроектирована с учётом требований Федерального закона № 152-ФЗ «О
персональных данных» [8] и Федерального закона № 149-ФЗ «Об информации,
информационных технологиях и о защите информации» [9]. Методология оценки
достаточности реализованных мер защиты опирается на положения ГОСТ Р
ИСО/МЭК 15408-3-2008 [10].

В рамках программного решения реализован следующий комплекс мер по
обеспечению информационной безопасности и защите персональных данных:

\begin{itemize}
  \item \textbf{Фиксация согласия на обработку персональных данных} --
        форма регистрации предусматривает обязательное подтверждение
        согласия пользователя посредством интерактивного элемента
        (чекбокса) с активной гиперссылкой на текст политики
        конфиденциальности. Отправка формы клиентом блокируется до
        момента подтверждения; аналогичный механизм валидации согласия
        реализован для сценария авторизации через внешний сервис VK ID.
  \item \textbf{Публикация правовых документов} -- на клиентском маршруте
        \texttt{/privacy} размещена страница политики конфиденциальности,
        структурированная в соответствии с требованиями 152-ФЗ (определены
        правовые основания, категории обрабатываемых данных, цели
        обработки, права субъектов, условия применения аналитических
        инструментов и контактные данные оператора).
  \item \textbf{Информирование об использовании cookie-файлов и сервисов
        аналитики} -- в п.\,8 политики конфиденциальности раскрыты
        сведения об обработке cookie-файлов и интеграции со счётчиком
        Яндекс.Метрики (5.4).
  \item \textbf{Безопасность хранения и управления данными} --
        аутентификационные данные не сохраняются в открытом виде: в базе
        данных фиксируются исключительно односторонние криптографические
        хеши (поле \texttt{password\_hash}). Физическое удаление учётной
        записи пользователя при наличии связанных с ней записей на приём
        предотвращается на уровне схемы реляционной БД с помощью
        ограничения ссылочной целостности (\texttt{ON DELETE RESTRICT}).
        Для прекращения обслуживания учётной записи применяется механизм
        логической блокировки (флаг \texttt{is\_blocked}).
\end{itemize}

% ============================================================
\gostsection{3}{Обоснование выбора средств реализации}
% ============================================================

% ------------------------------------------------------------------
% Старая таблица (стек и текстовое обоснование одной строкой на
% средство) оставлена закомментированной как черновой вариант --
% заменена полноценным анализом по методу экспертных оценок ниже
% (подразделы 3.1--3.3).
% ------------------------------------------------------------------
% Выбранный технологический стек и обоснование каждого решения приведены в
% таблице~1; ссылки в столбце «Средство» указывают на официальную
% документацию соответствующего инструмента.
%
% \begin{xltabular}{\textwidth}{|M|D|}
% \caption{Технологический стек и обоснование выбора} \\
% \hline
% \textbf{Средство} & \textbf{Обоснование} \\
% \hline
% \endfirsthead
% \multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
% \hline
% \textbf{Средство} & \textbf{Обоснование} \\
% \hline
% \endhead
% \hline
% \endfoot
% FastAPI (Python) [1] & Асинхронный фреймворк с валидацией запросов через
%   Pydantic «из коробки» и автогенерацией интерактивной документации
%   API (OpenAPI/Swagger, \texttt{/api/docs}) без отдельного описания
%   контракта вручную. \\\hline
% PostgreSQL 16 [3] & Единственная СУБД, рассмотренная всерьёз, -- из-за
%   ограничения целостности \texttt{EXCLUDE USING gist}: требование
%   «0 двойных бронирований» (техническое задание, раздел 2.2) решено
%   \emph{на уровне схемы БД}, а не проверкой в коде приложения перед
%   записью, которая при параллельных запросах подвержена состоянию
%   гонки. \\\hline
% SQLAlchemy 2.0 [2] + Alembic [11] & Типизированный ORM (\texttt{Mapped[...]})
%   и версионируемые миграции схемы вместо ручных SQL-скриптов. \\\hline
% Pydantic [7] & Валидация входных данных и сериализация ответов единым
%   механизмом, общим с автогенерацией документации FastAPI. \\\hline
% Vue 3 (Composition API) [6] + Pinia & Компактный реактивный фронтенд без
%   избыточного бойлерплейта Vuex/Redux -- оправдано масштабом проекта. \\\hline
% Tailwind CSS & Единая цветовая и типографическая система без написания
%   собственного CSS-фреймворка с нуля. \\\hline
% Docker Compose [12] + Caddy [13] & Воспроизводимое развёртывание и
%   автоматический TLS-сертификат Let's Encrypt без ручной настройки
%   nginx/certbot; подробности -- руководство администратора, раздел 1. \\
% \end{xltabular}

\gostsubsection{3.1}{Метод экспертных оценок}

Для обоснованного выбора технологического стека применяется метод
ранжировки с расчётом интегрального показателя полезности. Метод
позволяет формализовать экспертную оценку при сравнении альтернатив по
нескольким критериям с различным приоритетом; экспертом выступает
единственный исполнитель проекта.

Коэффициент ценности $C_{ij}$ для произвольного ранга критерия $r_{ij}$
($1 \leqslant r_{ij} \leqslant n$):
\[
C_{ij} = 1 - \frac{r_{ij} - 1}{n}, \tag{3.1}
\]
\noindent
где $C_{ij}$ -- коэффициент ценности $i$-го критерия;\\
$r_{ij}$ -- ранг критерия (1,0 -- наиболее значимый);\\
$n$ -- общее число критериев.

Нормированный вес $i$-го критерия $b_i$:
\[
b_i = C_{ij} \Big/ \sum_{i=1}^{n} C_{ij}. \tag{3.2}
\]

Интегральный показатель полезности альтернативы $E$:
\[
E = \sum_{i=1}^{n} b_i q_i, \tag{3.3}
\]
\noindent
где $E$ -- интегральный показатель полезности альтернативы;\\
$b_i$ -- нормированный вес $i$-го критерия;\\
$q_i \in [0;\,1]$ -- нормированная оценка альтернативы по $i$-му критерию
(1,00 присваивается лучшей альтернативе по данному критерию).

\gostsubsection{3.2}{Список альтернатив}

Рассматриваются технологии с открытой лицензией и широкой
распространённостью на российском рынке разработки; учитывается
доступность специалистов и отсутствие лицензионных ограничений для
разработчиков в РФ.

\textbf{Серверная часть.} Рассматриваются Python, Node.js, Java и PHP.
Python обладает развитой экосистемой библиотек для работы с базами
данных, построения REST API и организации фоновых задач. Node.js --
популярная платформа для серверной разработки на JavaScript с активным
сообществом. Java -- зрелое корпоративное решение с богатой
инфраструктурой, но с более высоким порогом входа и длительной
первоначальной настройкой. PHP сохраняет значительную долю рынка
корпоративных веб-приложений. При выборе серверной части приоритет
отдан доступности специалистов и скорости разработки: нагрузка малой
парикмахерской или сети из нескольких точек не является критичным
фактором, поэтому производительность и масштабируемость имеют меньший
вес. Критерии оценивания серверной части приведены в соответствии с таблицей~1.

\begin{table}[H]
\centering
\begin{gosttable}{Критерии и веса оценки серверной части}
\begin{tabular}{|c|T|c|c|c|}
\hline
\textbf{Обозначение} & \multicolumn{1}{c|}{\textbf{Критерий}} & \textbf{Ранг $r$} & \textbf{$C_i$} & \textbf{$b_i$} \\
\hline
$q_1$ & Производительность & 6,5 & 0,313 & 0,070 \\\hline
$q_2$ & Надёжность, наличие готовых библиотек & 4 & 0,625 & 0,139 \\\hline
$q_3$ & Стоимость и лицензирование & 3 & 0,750 & 0,167 \\\hline
$q_4$ & Безопасность & 2 & 0,875 & 0,194 \\\hline
$q_5$ & Документация и сообщество & 8 & 0,125 & 0,028 \\\hline
$q_6$ & Масштабируемость & 6,5 & 0,313 & 0,070 \\\hline
$q_7$ & Скорость разработки & 1 & 1,000 & 0,222 \\\hline
$q_8$ & Простота интеграции со сторонними сервисами & 5 & 0,500 & 0,111 \\\hline
\end{tabular}
\end{gosttable}
\end{table}

\textbf{Клиентская часть.} Рассматриваются Vue 3, React, Next.js и
Angular. Vue 3 и React -- наиболее распространённые библиотеки для
построения одностраничных приложений (SPA). Next.js -- фреймворк на
основе React с поддержкой серверного рендеринга (SSR), избыточного для
проекта без требований к SEO публичной части. Angular -- полноценный
фреймворк со встроенной маршрутизацией, управлением состоянием и
внедрением зависимостей, но с более высоким порогом входа. При выборе
клиентской части приоритет отдан скорости разработки UI и низкому
порогу входа: система включает как публичный интерфейс записи, так и
административную панель, разрабатываемые одним исполнителем. Критерии
оценивания клиентской части приведены в соответствии с таблицей~2.

\begin{table}[H]
\centering
\begin{gosttable}{Критерии и веса оценки клиентской части}
\begin{tabular}{|c|T|c|c|c|}
\hline
\textbf{Обозначение} & \multicolumn{1}{c|}{\textbf{Критерий}} & \textbf{Ранг $r$} & \textbf{$C_i$} & \textbf{$b_i$} \\
\hline
$q_1$ & Скорость разработки UI, компактность API реактивности & 1 & 1,000 & 0,400 \\\hline
$q_2$ & Порог входа для стороннего разработчика & 2 & 0,750 & 0,300 \\\hline
$q_3$ & Сложность интеграции с backend (REST API) & 3 & 0,500 & 0,200 \\\hline
$q_4$ & Качество документации & 4 & 0,250 & 0,100 \\\hline
\end{tabular}
\end{gosttable}
\end{table}

\textbf{Хранение данных.} Рассматриваются PostgreSQL, MariaDB и SQLite.
Все три СУБД распространяются по открытым лицензиям. PostgreSQL --
объектно-реляционная СУБД с развитыми механизмами обеспечения
целостности данных и конкурентного доступа. MariaDB -- развитие MySQL,
широко применяемое в веб-разработке. SQLite -- встраиваемая СУБД без
отдельного серверного процесса, с существенными ограничениями в части
конкурентной записи. При выборе СУБД целостность данных и конкурентный
доступ -- первостепенный критерий: несколько администраторов и клиентов
одновременно работают с расписанием, и двойное бронирование одного
временного слота должно быть исключено средствами самой СУБД
(многоверсионное управление конкурентным доступом, MVCC; уровни изоляции
транзакций; ограничения целостности), а не только
проверкой в коде приложения. Критерии оценивания СУБД приведены в
соответствии с таблицей~3.

\begin{table}[H]
\centering
\begin{gosttable}{Критерии и веса оценки СУБД}
\begin{tabular}{|c|T|c|c|c|}
\hline
\textbf{Обозначение} & \multicolumn{1}{c|}{\textbf{Критерий}} & \textbf{Ранг $r$} & \textbf{$C_i$} & \textbf{$b_i$} \\
\hline
$q_1$ & Поддержка конкурентного доступа и изоляции транзакций & 1 & 1,000 & 0,279 \\\hline
$q_2$ & Ограничения целостности на уровне схемы (\texttt{EXCLUDE}, диапазонные типы) & 2 & 0,833 & 0,233 \\\hline
$q_3$ & Простота администрирования и резервного копирования & 3 & 0,667 & 0,186 \\\hline
$q_4$ & Поддержка со стороны российских managed-хостингов & 4 & 0,500 & 0,140 \\\hline
$q_5$ & Производительность при индексируемых выборках & 5 & 0,333 & 0,093 \\\hline
$q_6$ & Лицензия и отсутствие санкционных ограничений & 6 & 0,167 & 0,047 \\\hline
\end{tabular}
\end{gosttable}
\end{table}

\gostsubsection{3.3}{Итоговые средства реализации}

Итоговые результаты экспертной оценки представлены в соответствии с таблицей~4.

\begin{table}[H]
\centering
\begin{gosttable}{Итоговые результаты экспертной оценки}
\begin{tabular}{|l|l|c|}
\hline
\multicolumn{1}{|c|}{\textbf{Категория}} & \multicolumn{1}{c|}{\textbf{Средство}} & \textbf{$E$} \\
\hline
Серверная часть & Python (FastAPI) & 0,904 \\\hline
Клиентская часть & Vue 3 & 0,930 \\\hline
СУБД & PostgreSQL & 0,978 \\\hline
\end{tabular}
\end{gosttable}
\end{table}

Python демонстрирует наивысший интегральный показатель (0,904) прежде
всего за счёт лидерства по критерию скорости разработки, имеющему
наибольший вес (0,222). Фреймворк FastAPI [1] обеспечивает встроенную
валидацию данных через Pydantic [7], автоматическую генерацию
интерактивной документации API (OpenAPI/Swagger, \texttt{/api/docs}) и
асинхронную обработку запросов -- полноценный REST API реализуется за
минимальное время без отдельного описания контракта вручную. Экосистема
Python предоставляет зрелые библиотеки для всех задач проекта:
SQLAlchemy 2.0 [2] (типизированный ORM), Alembic [11] (версионируемые
миграции схемы), python-jose (JWT). Весь стек распространяется под
открытыми лицензиями без ограничений для российских разработчиков.

Фреймворк Vue 3 занял лидирующую позицию с интегральным показателем
0,930 благодаря максимальной оценке по критерию скорости разработки
пользовательского интерфейса (весовой коэффициент 0,400). Использование
Composition API в сочетании с библиотекой управления состоянием Pinia
позволяет создавать реактивный интерфейс без избыточного шаблонного
кода (boilerplate), характерного для связок Vuex/Redux или системы
внедрения зависимостей Angular, что оптимально для масштаба
разрабатываемого сервиса. Интеграция с REST API бэкенда стандартизирована,
а исчерпывающая документация фреймворка минимизирует порог входа и
упрощает сопровождение клиентской части.

PostgreSQL демонстрирует наивысший интегральный показатель (0,978) и
уверенно лидирует по двум наиболее весомым критериям. По конкурентному
доступу PostgreSQL не имеет равных среди рассмотренных альтернатив:
механизм MVCC и поддержка уровней изоляции транзакций гарантируют
корректность при одновременных попытках бронирования одного слота
несколькими клиентами. Дополнительную защиту обеспечивает ограничение
целостности \texttt{EXCLUDE USING gist} с диапазонным типом
\texttt{tstzrange}: требование «0 двойных бронирований» (техническое
задание, 2.2) решено \emph{на уровне схемы БД}, а не проверкой в
коде приложения перед записью, которая при параллельных запросах
подвержена состоянию гонки. PostgreSQL распространяется под лицензией
PostgreSQL License (аналог MIT) и доступен в виде managed-сервиса на
отечественных облачных платформах. SQLite исключён ввиду отсутствия
полноценной поддержки конкурентной записи; MariaDB уступает PostgreSQL
по глубине поддержки механизмов изоляции транзакций и диапазонных
ограничений целостности.

Вспомогательные средства реализации, не участвовавшие в сравнении
альтернатив ввиду отсутствия конкурирующих решений того же назначения в
рамках проекта, -- Tailwind CSS (единая цветовая и типографическая
система без написания собственного CSS-фреймворка с нуля) и Docker
Compose [12] с Caddy [13] (воспроизводимое развёртывание и
автоматический TLS-сертификат Let's Encrypt без ручной настройки
nginx/certbot; подробности -- руководство администратора, раздел 1).

\textbf{Обоснование отказа от распределённой очереди задач (Celery /
Redis).} Рассылка уведомлений клиентам реализована в виде фонового
асинхронного цикла (\texttt{asyncio}) внутри основного процесса бэкенда
без привлечения внешнего брокера сообщений (техническое задание,
3.3). Архитектура системы рассчитана на эксплуатацию в рамках
одного контейнера \texttt{backend} (руководство администратора,
раздел 1). В этих условиях внедрение внешнего брокера очередей привело
бы к неоправданному усложнению инфраструктуры и увеличению накладных
расходов. По аналогичной причине ограничение частоты запросов (rate
limiting) и защита от подбора аутентификационных данных реализованы без
использования внешнего кэша Redis: лимиты запросов контролируются
библиотекой \texttt{slowapi} в оперативной памяти процесса по
IP-адресам, а фиксация попыток входа осуществляется в служебной таблице
\texttt{login\_attempts} базы данных.

\gostsubsection{3.4}{Дополнительные инструменты разработки}

Вспомогательные инструменты линтинга, тестирования и непрерывной
интеграции, использованные при разработке, перечислены в соответствии с таблицей~5.

\begin{xltabular}{\textwidth}{|M|D|}
\caption{Инструменты разработки, тестирования и контроля качества} \\
\hline
\multicolumn{1}{|c|}{\textbf{Инструмент}} & \multicolumn{1}{c|}{\textbf{Назначение}} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\multicolumn{1}{|c|}{\textbf{Инструмент}} & \multicolumn{1}{c|}{\textbf{Назначение}} \\
\hline
\endhead
\endfoot
ruff & Линтер Python (\texttt{E,F,B,UP,SIM,C4}). \\\hline
ESLint 9 + eslint-plugin-vue & Линтер JS/Vue (flat config,
  \envvar{vue/flat/essential}). \\\hline
Pyright & Проверка типов Python в режиме \texttt{basic} (локально). \\\hline
pytest (unit + integration) & Модульные тесты (\envvar{backend/tests})
  и отдельно помеченные интеграционные тесты
  (\envvar{backend/tests/integration}) на настоящем Postgres через
  \texttt{httpx.AsyncClient} -- покрывают ограничение \texttt{EXCLUDE},
  уникальность slug точки, область видимости по точкам сети, полный
  сценарий записи «от начала до конца». \\\hline
Vitest + @vue/test-utils & Модульные тесты фронтенда (хранилища,
  composables). \\\hline
Playwright + @axe-core/playwright & End-to-end тесты в браузере
  (Chromium) и автоматическая регрессия по доступности (accessibility). \\\hline
GitHub Actions (CI) & Параллельные job'ы на каждый push/PR в
  \texttt{main}: тесты и линт бэкенда, интеграционные тесты бэкенда на
  реальном Postgres, сборка и линт фронтенда, unit- и e2e-тесты
  фронтенда, синтаксическая проверка \envvar{docker-compose.yml}. \\\hline
pip-audit / npm audit & Проверка зависимостей на известные уязвимости
  как отдельные шаги CI. \\\hline
Dependabot & Еженедельные автоматические PR на обновление зависимостей
  (pip, npm, Docker-образы обоих Dockerfile, GitHub Actions). \\\hline
\end{xltabular}

% ============================================================
\gostsection{4}{Структура базы данных}
% ============================================================

\gostsubsection{4.1}{Инфологическая модель}

Модель разделена на две схемы для читаемости: в соответствии с рисунком~2 --
сущности бизнес-домена (точки, пользователи, мастера, услуги, записи,
отзывы); в соответствии с рисунком~3 -- сущности, обеспечивающие
безопасность входа (сессии, токены, попытки входа), и настройки сайта.

\begin{sidewaysfigure}[htbp]
\centering
\includegraphics[height=\textwidth, width=\textheight, keepaspectratio]{Images/ER_final_1.png}
\caption{Инфологическая модель -- бизнес-домен (актуальная схема БД)}
\end{sidewaysfigure}

\begin{sidewaysfigure}[htbp]
\centering
\includegraphics[height=\textwidth, width=\textheight, keepaspectratio]{Images/ER_final_2.png}
\caption{Инфологическая модель -- аутентификация и настройки сайта}
\end{sidewaysfigure}

% \clearpage выталкивает оба повёрнутых рисунка до начала подраздела 4.2:
% иначе они всплывают уже после его текста и отрываются от подраздела 4.1,
% к которому относятся (п. 8.2 -- рисунок помещается сразу после ссылки).
\clearpage

\gostsubsection{4.2}{Даталогическая модель}

Фактическая схема БД насчитывает 13 таблиц (15 миграций Alembic [11] в
директории \envvar{backend/alembic/versions/}):

\begin{enumerate}
  \item \texttt{salons} -- точки сети (адрес, часы работы, slug);
  \item \texttt{users} -- учётные записи всех ролей (client/master/admin/owner);
  \item \texttt{masters} -- профиль мастера (специализация, коэффициент к цене);
  \item \texttt{schedules} -- рабочее расписание мастера по дням недели;
  \item \texttt{services} -- каталог услуг (цена, длительность);
  \item \texttt{master\_services} -- прайс услуги, индивидуальный по мастеру;
  \item \texttt{appointments} -- записи на приём (\texttt{EXCLUDE USING gist}
        -- защита от двойного бронирования на уровне СУБД);
  \item \texttt{reviews} -- отзывы клиентов о завершённых записях;
  \item \texttt{sessions} -- серверное хранилище refresh-сессий;
  \item \envvar{password\_reset\_tokens} -- одноразовые токены сброса пароля;
  \item \envvar{email\_verification\_tokens} -- одноразовые токены
        подтверждения email;
  \item \texttt{login\_attempts} -- аудит попыток входа;
  \item \texttt{site\_settings} -- редактируемый контент сайта (единственная
        строка, JSONB).
\end{enumerate}

Даталогическая модель данных приведена в соответствии с рисунком~4.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth,height=0.95\textheight,keepaspectratio]{Images/db_diagram.png}
    \caption{Даталогическая модель -- физическая схема БД}
\end{figure}

% ============================================================
\gostsection{5}{Реализация и развёртывание приложения}
% ============================================================

\gostsubsection{5.1}{Общие сведения}

Полный перечень операций по каждой из четырёх ролей -- руководство
пользователя, 3.4.1--3.4.4. Здесь -- сквозные технические сценарии
двух ключевых процессов на уровне «фронтенд $\to$ бэкенд $\to$ БД»;
диаграммы последовательностей пяти ключевых технических сценариев,
детализирующих часть процессов из 1.2, приведены в приложении А.

\gostsubsection{5.2}{Создание записи (бронирование)}

Технический сценарий создания записи на уровне «фронтенд $\to$ бэкенд
$\to$ БД» показан в соответствии с рисунком~5.

\begin{figure}[htbp]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент\\(браузер, Vue)}
  \seqhead{4.6}{Backend}
  \seqhead{9.2}{PostgreSQL}

  \seqmsgB{-0.1}{4.7}{GET /api/masters/\{id\}/slots\\?service\_id\&date}
  \seqmsgB{4.5}{9.2}{SELECT расписания\\и записей мастера}
  \seqmark{\dTopA}
  \seqretA{9.2}{4.5}{строки результата}
  \seqmark{\dBotA}
  \seqact{9.2}{\dTopA}{\dBotA}
  \seqretA{4.7}{-0.1}{список свободных слотов}

  \seqmsgB{-0.1}{4.6}{POST /api/appointments\\(слот, услуга, мастер)}
  \seqmark{\bTop}
  \seqmsgB{4.6}{9.2}{INSERT в appointments\\(EXCLUDE USING gist)}
  \seqmark{\dTopB}
  \seqretB{9.2}{4.6}{успех / нарушение EXCLUDE\\(конфликт диапазона)}
  \seqmark{\dBotB}
  \seqact{9.2}{\dTopB}{\dBotB}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{2.6}{конфликт диапазона}
  \seqretA{4.6}{-0.1}{409 Конфликт}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{2.6}{успех}
  \seqretA{4.6}{-0.1}{201 Создано}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.7}{9.9}{\altTop}{\altBot}
  \seqsep{-0.7}{\sepA}{4.5}
  \seqsep{4.7}{\sepA}{9.9}
  \seqmark{\bBot}
  \seqact{4.6}{\bTop}{\bBot}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент\\(браузер, Vue)}{\finalY}
  \seqfoot{4.6}{Backend}{\finalY}
  \seqfoot{9.2}{PostgreSQL}{\finalY}
\end{tikzpicture}%
}
\caption{Диаграмма последовательности: создание записи}
\end{figure}

Повторная проверка доступности слота на шаге 6 на уровне СУБД, а не только
на уровне кода перед вставкой, -- прямая реализация цели «0 инцидентов
двойного бронирования» из технического задания (2.2): при
конкурентной попытке два запроса могут одновременно пройти проверку на
шаге 1--4, но лишь один пройдёт \texttt{INSERT} на шаге 6, второй получит
409 от самой базы данных.

\gostsubsection{5.3}{Первый запуск системы (мастер \texttt{/setup})}

Сценарий первого запуска, выполняемый мастером настройки \texttt{/setup},
представлен в соответствии с рисунком~6.

\begin{figure}[htbp]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Браузер\\(/setup)}
  \seqhead{4.6}{Backend}
  \seqhead{9.2}{PostgreSQL}

  \seqmsgA{-0.1}{4.7}{GET /api/setup/status}
  \seqretA{4.7}{-0.1}{настройка не выполнена}
  \seqmsgB{-0.1}{4.6}{POST /api/setup\\(владелец, точка, брендинг)}
  \seqmark{\bTop}
  \seqmsgB{4.6}{9.2}{INSERT владельца, точки\\и настроек -- одной транзакцией}
  \seqmark{\dTop}
  \seqretA{9.2}{4.6}{успех}
  \seqmark{\dBot}
  \seqact{9.2}{\dTop}{\dBot}
  \seqretB{4.6}{-0.1}{сессия владельца,\\редирект в панель}
  \seqmark{\bBot}
  \seqact{4.6}{\bTop}{\bBot}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Браузер\\(/setup)}{\finalY}
  \seqfoot{4.6}{Backend}{\finalY}
  \seqfoot{9.2}{PostgreSQL}{\finalY}
\end{tikzpicture}%
}
\caption{Диаграмма последовательности: первый запуск (\texttt{/setup})}
\end{figure}

Маршрут \texttt{/setup} проверяется единым глобальным
\envvar{router.beforeEach}-хуком фронтенда перед \emph{каждой}
навигацией (раздел 2), поэтому попасть в остальную часть приложения,
минуя этот сценарий, нельзя; после успешного шага 6 статус настройки
самоблокируется навсегда (руководство администратора, 3.4).

\gostsubsection{5.4}{Подключение аналитики}

В системе три независимых источника аналитики, которые не стоит путать
между собой:

\begin{itemize}
  \item \textbf{Веб-аналитика (Яндекс.Метрика) -- подключена и активна.}
        Счётчик загружается отдельным статическим файлом
        (\texttt{/metrika.js}), а не инлайн-скриптом в
        \texttt{index.html}, -- ради политики безопасности контента (CSP):
        \envvar{script-src} остаётся \texttt{'self'} без исключения
        \envvar{'unsafe-inline'}. Использование раскрыто в §8 политики
        конфиденциальности (2.4).
  \item \textbf{Мониторинг ошибок (Sentry) -- код готов, отключён по
        умолчанию.} Инициализация присутствует и на бэкенде, и на
        фронтенде, но выполняется только при заданном
        \texttt{SENTRY\_DSN}/\texttt{VITE\_SENTRY\_DSN}.
  \item \textbf{Бизнес-аналитика -- собственные отчёты по данным
        приложения.} Раздел «Статистика» (KPI-плитки, график регистраций
        за 30 дней) и раздел «Отчёты» (выручка и загруженность по
        дням/мастерам/услугам с экспортом в Excel) -- оба на данных из
        собственной БД проекта, без внешнего сервиса. Реализуют цель
        технического задания «формирование отчёта по выручке и
        загруженности без привлечения разработчика» (2.2); полное
        описание экранов -- руководство пользователя, 3.4.3.
\end{itemize}

\gostsubsection{5.5}{Размещение в сети Интернет}

Полный порядок развёртывания, включая перечень необходимых портов,
подключение домена и TLS, первичную настройку и восстановление после
сбоя, -- предмет отдельного документа (руководство администратора,
разделы 1--4) и здесь не повторяется. Кратко: система разворачивается
как согласованный набор Docker-контейнеров [12] (база данных, backend,
frontend, реверс-прокси Caddy [13] с автоматическим TLS-сертификатом
Let's Encrypt, ежедневное резервное копирование), наружу из этой
топологии открыты только порты 80/443 контейнера Caddy, обновление
выполняется вручную одной командой (\envvar{scripts/deploy.sh}) без
автоматического деплоя по CI (3.4).

% ============================================================
\section*{\hspace*{\gostindent}ЗАКЛЮЧЕНИЕ}
\addcontentsline{toc}{section}{ЗАКЛЮЧЕНИЕ}
% ============================================================

В ходе разработки создана и доведена до готовности к развёртыванию CMS
«Сайтама» -- веб-приложение для автоматизации онлайн-записи и
операционной деятельности сети барбершопов.

\textbf{Соответствие целям технического задания.} Ключевые измеримые цели
(2.2 технического задания) обеспечены технически: двойное
бронирование исключено ограничением целостности СУБД, а не
организационно (раздел 5); индивидуальный прайс по мастеру, единое
хранилище данных и отчёт по выручке/загруженности без участия
разработчика -- реализованы и доступны в интерфейсе администратора
(5.4). Часть целей (снижение доли неявок за счёт напоминаний, доля
клиентов, выбирающих вход через VK ID) сформулирована как показатель,
измеримый только на реальной эксплуатации.

\textbf{Направления дальнейшего развития.} Второй OAuth-провайдер и
дополнительные каналы уведомлений можно добавить без изменения
архитектуры (раздел 2 уже отделяет бизнес-логику сервисов от конкретного
канала доставки/провайдера); горизонтальное масштабирование backend
потребует пересмотра внутрипроцессных механизмов защиты от перебора
паролей и ограничения частоты запросов (раздел 3) в пользу общего
хранилища состояния; полноценный журнал аудита -- отдельная таблица с
записью каждой критичной операции, а не только попыток входа.

Итоговое состояние проекта, полный перечень функциональных требований и
инструкции по эксплуатации -- см. соответственно техническое задание,
руководство пользователя и руководство администратора; весь комплект
документации оформлен с учётом общих требований к программным документам
[14].

% ============================================================
\section*{\hspace*{\gostindent}БИБЛИОГРАФИЧЕСКИЙ СПИСОК}
\addcontentsline{toc}{section}{БИБЛИОГРАФИЧЕСКИЙ СПИСОК}
% ============================================================

% Арабская нумерация здесь предписана отдельно (п. 14.2 методического
% пособия), поэтому список набран numlist, а не буквенным enumerate.
\begin{numlist}
  \item FastAPI -- официальная документация [Электронный ресурс]. --
        URL: \url{https://fastapi.tiangolo.com/} (дата обращения: 29.08.2026).
  \item SQLAlchemy 2.0 -- официальная документация [Электронный
        ресурс]. -- URL: \url{https://docs.sqlalchemy.org/en/20/} (дата
        обращения: 29.08.2026).
  \item PostgreSQL 16 -- официальная документация [Электронный
        ресурс]. -- URL: \url{https://www.postgresql.org/docs/16/} (дата
        обращения: 29.08.2026).
  \item ГОСТ 34.602-2020. Информационные технологии. Комплекс
        стандартов на автоматизированные системы. Техническое задание
        на создание автоматизированной системы. -- М.: Стандартинформ,
        2020.
  \item ГОСТ 19.201-78. Единая система программной документации.
        Техническое задание. Требования к содержанию и оформлению. --
        М.: Изд-во стандартов, 1978.
  \item Vue.js -- официальная документация [Электронный ресурс]. --
        URL: \url{https://vuejs.org/} (дата обращения: 29.08.2026).
  \item Pydantic -- официальная документация [Электронный ресурс]. --
        URL: \url{https://docs.pydantic.dev/} (дата обращения: 29.08.2026).
  \item Федеральный закон от 27.07.2006 № 152-ФЗ «О персональных
        данных».
  \item Федеральный закон от 27.07.2006 № 149-ФЗ «Об информации,
        информационных технологиях и о защите информации».
  \item ГОСТ Р ИСО/МЭК 15408-3-2008. Информационная технология. Методы
        и средства обеспечения безопасности. Критерии оценки
        безопасности информационных технологий. Часть 3. -- М.:
        Стандартинформ, 2008.
  \item Alembic -- официальная документация [Электронный ресурс]. --
        URL: \url{https://alembic.sqlalchemy.org/} (дата обращения:
        29.08.2026).
  \item Docker Compose -- официальная документация [Электронный
        ресурс]. -- URL: \url{https://docs.docker.com/compose/} (дата
        обращения: 29.08.2026).
  \item Caddy -- официальная документация [Электронный ресурс]. --
        URL: \url{https://caddyserver.com/docs/} (дата обращения:
        29.08.2026).
  \item ГОСТ 19.105-78. Единая система программной документации. Общие
        требования к программным документам. -- М.: Изд-во стандартов,
        1978.
\end{numlist}

% ============================================================
\gostappendix{А}{Диаграммы последовательностей}
% ============================================================
\renewcommand{\thefigure}{А.\arabic{figure}}
\setcounter{figure}{0}

Рисунки А.1--А.5 иллюстрируют порядок взаимодействия участников для ключевых
бизнес-процессов системы, детализированных в карточках функциональных
требований технического задания (4.2).

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgB{-0.1}{4.5}{Заполнение формы регистрации\\(email, пароль, имя...)}
  \seqmsgA{4.3}{9.0}{POST /api/auth/register}
  \seqmark{\cTop}
  \seqmsgB{9.0}{13.4}{Проверка уникальности\\email/телефона}
  \seqmark{\dTopA}
  \seqretA{13.4}{9.0}{Результат проверки}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{email или телефон уже заняты}
  \seqretB{9.0}{4.3}{409 Пользователь с таким\\email уже существует}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{email и телефон свободны}
  \seqselfA{9.0}{Хеширование пароля}
  \seqmsgA{9.0}{13.4}{INSERT в users}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{Пользователь создан}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqselfA{9.0}{Генерация JWT\\access/refresh}
  \seqselfB{9.0}{Токен подтверждения email,\\отправка письма}
  \seqretA{9.0}{4.3}{201 Created + JWT-токены}
  \seqmark{\cBot}
  \seqact{9.0}{\cTop}{\cBot}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}

  \seqmsgB{4.5}{-0.1}{Переход в личный кабинет\\(клиент уже вошёл)}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Регистрация» (ФТ-001)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgA{-0.1}{4.5}{Ввод email и пароля}
  \seqmsgA{4.3}{9.0}{POST /api/auth/login}
  \seqmark{\cTop}
  \seqmsgB{9.0}{13.4}{Поиск пользователя\\по email}
  \seqmark{\dTopA}
  \seqretB{13.4}{9.0}{Данные пользователя\\(password\_hash) либо не найден}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{польз. не найден /\\ вход временно заблокирован}
  \seqretA{9.0}{4.3}{401 Неверный email или пароль}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{пользователь найден,\\блокировки нет}
  \seqselfA{9.0}{Проверка пароля (bcrypt)}

  \seqpad{0.35}\seqmark{\altBTop}
  \seqcond{7.0}{пароль неверный}
  \seqmsgA{9.0}{13.5}{Запись неудачной попытки входа}
  \seqretA{9.0}{4.3}{401 Неверный пароль}
  \seqpad{0.3}\seqmark{\sepB}
  \seqpad{0.3}
  \seqcond{7.0}{пароль верный}
  \seqselfB{9.0}{Генерация JWT access\\+ refresh-сессии}
  \seqmsgA{9.0}{13.4}{Сохранение сессии\\(хеш refresh-токена)}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{OK}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretB{9.0}{4.3}{200 + access-токен;\\refresh в httpOnly cookie}
  \seqmark{\cBot}
  \seqpad{0.35}\seqmark{\altBBot}
  \seqframe{alt}{0.4}{13.6}{\altBTop}{\altBBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}

  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTop}{\cBot}

  \seqmsgA{4.5}{-0.1}{Переход в личный кабинет}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Вход по email и паролю»}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgA{-0.1}{4.5}{Выбор мастера, услуги и даты}
  \seqmsgB{4.3}{9.0}{GET /api/slots?master\_id\\\&service\_id\&date}
  \seqmark{\cTopA}
  \seqmsgB{9.0}{13.4}{Запрос расписания\\и записей мастера}
  \seqmark{\dTopA}
  \seqretA{13.4}{9.0}{Расписание + список записей}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}
    \seqselfB{9.0}{Расчёт свободных слотов\\шаг = длительность услуги}
  \seqretA{9.0}{4.3}{Список свободных слотов}
  \seqmark{\cBotA}
  \seqact{9.0}{\cTopA}{\cBotA}

  \seqmsgA{4.5}{-0.1}{Отображение слотов}
  \seqmsgB{-0.1}{4.5}{Выбор слота,\\подтверждение записи}
  \seqmsgA{4.3}{9.0}{POST /api/appointments}
  \seqmark{\cTopB}
  \seqselfB{9.0}{Проверки: лимит, email,\\расписание, будущее время}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{нарушена проверка /\\ конфликт слота}
  \seqretA{9.0}{4.3}{400/409 ошибка}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{все проверки пройдены}
  \seqmsgB{9.0}{13.4}{INSERT в appointments\\(EXCLUDE USING gist)}
  \seqmark{\dTopB}
  \seqretB{13.4}{9.0}{Запись создана\\(status=pending)}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretA{9.0}{4.3}{201 Created + данные записи}
  \seqmark{\cBotB}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTopB}{\cBotB}

  \seqmsgB{4.5}{-0.1}{«Запись создана,\\ожидает подтверждения»}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Онлайн-запись на приём» (ФТ-004, ФТ-005)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgB{-0.1}{4.5}{Нажатие «Отменить запись»\\в личном кабинете}
  \seqmsgA{4.3}{9.0}{POST /api/appointments/\{id\}/cancel}
  \seqmark{\cTop}
  \seqmsgA{9.0}{13.4}{Поиск записи по id}
  \seqmark{\dTopA}
  \seqretB{13.4}{9.0}{Данные записи\\(client\_id, статус)}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{запись принадлежит\\другому клиенту}
  \seqretB{9.0}{4.3}{Можно отменить только\\свою запись}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{статус не допускает отмену}
  \seqretA{9.0}{4.3}{400 ошибка с текущим статусом}
  \seqpad{0.3}\seqmark{\sepB}
  \seqpad{0.3}
  \seqcond{6.7}{запись своя,\\статус допускает отмену}
  \seqmsgA{9.0}{13.4}{UPDATE status = cancelled}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{Успех}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretA{9.0}{4.3}{200 + обновлённый статус}
  \seqmark{\cBot}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTop}{\cBot}

  \seqmsgA{4.5}{-0.1}{«Запись отменена»}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Отмена записи клиентом» (ФТ-006)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Планировщик}
  \seqhead{4.6}{База данных}
  \seqhead{9.2}{Email-сервис}

  \seqselfB{-0.1}{Пробуждение по таймеру\\(каждые 5 минут)}
  \seqmsgB{-0.1}{4.6}{Выборка записей с рубежом\\24ч/2ч и неотправл. напоминанием}
  \seqmark{\bTop}
  \seqretA{4.6}{-0.1}{Список записей}
  \seqmark{\bBot}
  \seqact{4.6}{\bTop}{\bBot}

  \seqpad{0.35}\seqmark{\loopTop}
  \seqcond{2.3}{для каждой записи в списке}
  \seqmark{\aTop}
  \seqpad{0.75}\seqmark{\seqy}\draw[seqmsg] ($(0,\seqy)!0.09cm!(9.2,\seqy)$) -- ($(9.2,\seqy)!0.09cm!(0,\seqy)$) node[pos=0.75, above] {Отправка письма-напоминания};
  \seqmark{\cTop}
  \seqpad{0.75}\seqmark{\seqy}\draw[seqret] ($(9.2,\seqy)!0.09cm!(0,\seqy)$) -- ($(0,\seqy)!0.09cm!(9.2,\seqy)$) node[pos=0.25, above] {Статус отправки};
  \seqmark{\cBot}
  \seqact{9.2}{\cTop}{\cBot}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{2.3}{успешно}
  \seqmsgA{0}{4.7}{UPDATE reminder\_24h/2h\_sent\_at}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{2.3}{ошибка отправки}
  \seqselfB{0}{Логирование ошибки;\\повтор на следующем цикле}
  \seqpad{0.35}\seqmark{\altBot}
  \seqframe{alt}{-0.7}{9.3}{\altTop}{\altBot}
  \seqsep{0.1}{\sepA}{9.3}

  \seqmark{\aBot}
  \seqact{0}{\aTop}{\aBot}
  \seqpad{0.35}\seqmark{\loopBot}
  \seqloopframe{-1}{9.6}{\loopTop}{\loopBot}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Планировщик}{\finalY}
  \seqfoot{4.6}{База данных}{\finalY}
  \seqfoot{9.2}{Email-сервис}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Автоматическая отправка напоминаний» (ФТ-008)}
\end{figure}

\end{document}
