% config.tex - преамбула документа (класс, пакеты, оформление по ГОСТ 2.105).
% Подключается из main.tex через \input{config}.
% Адаптировано из admin_manual/config.tex (та же визуальная схема, что и у
% всех документов проекта: класс, поля, шрифты, оформление таблиц/схем,
% включая ручную нумерацию разделов \gostsection/\gostsubsection/
% \gostsubsubsection) - без заглушек схем, все схемы в этом отчёте
% нарисованы по-настоящему (tikz), а не отмечены рамкой.

\documentclass[14pt]{extarticle}

% ---------- Кодировка и язык ----------
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{russian}
\setotherlanguage{english}

\setmainfont{Liberation Serif}      % метрический аналог Times New Roman, есть кириллица
\setsansfont{Liberation Sans}
\setmonofont{Liberation Serif}

% ---------- Поля и межстрочный интервал (ГОСТ 2.105) ----------
% Поля по п. 2.9 методического пособия СЛИ (лист без рамки, ГОСТ 7.32-2001):
% левое 30 мм, правое 10 мм, верхнее и нижнее по 20 мм.
\usepackage[a4paper,left=30mm,right=15mm,top=20mm,bottom=20mm]{geometry}
\usepackage{setspace}
\onehalfspacing
\usepackage{microtype}   % устраняет переполнение правого поля в justify-тексте
\emergencystretch=3em    % последний резерв: тянуть пробелы, а не вылезать за правое поле (актуально для \texttt-идентификаторов без переносов)

% ---------- Переносы слов (ГОСТ 2.105, п. 2.12: не рекомендуются) ----------
% Полный запрет (через lefthyphenmin/righthyphenmin) не годится: без переноса
% TeX не может разбить длинное слово, и строка вылезает за правое поле
% (overfull hbox -- реальный видимый дефект, хуже переноса). Вместо этого
% перенос делается штрафуемым по максимуму: TeX ищет любой другой способ
% (растяжение/сжатие пробелов) и прибегает к переносу, только если иначе
% строка физически не влезает в поле. Штраф именно 10000 (а не чуть меньше)
% TeX трактует как "точка переноса запрещена вовсе" (см. TeXbook) -- поэтому
% реальный disable получался тем же самым, что и через lefthyphenmin/
% righthyphenmin, и приводил к overfull-строкам. 5000 -- всё ещё огромный
% штраф (обычные слова почти никогда не переносятся), но конечный: перенос
% остаётся доступен как запасной вариант, когда иначе слово вылезет за поле.
\hyphenpenalty=5000
\exhyphenpenalty=5000
\tolerance=2000

% ---------- Абзацы ----------
% Абзацный отступ -- не менее 15 мм (пп. 2.6, 2.9 методического пособия СЛИ).
\setlength{\parindent}{1.5cm}
% Отдельная длина с тем же значением: titlesec обнуляет \parindent внутри
% заголовков, поэтому для отступа номера главы/подраздела нужна своя копия.
\newlength{\gostindent}
\setlength{\gostindent}{\parindent}
\setlength{\parskip}{0pt}

% ---------- Заголовки разделов ----------
% Каждый раздел верхнего уровня - с новой страницы; подразделы продолжают
% течь в потоке, без разрыва.
\usepackage{titlesec}
% \clearpage раньше стоял последним (пятым) аргументом \titleformat -
% "код перед заголовком" по документации titlesec. На практике в этой
% версии пакета \clearpage там выполняется МЕЖДУ номером раздела и его
% названием, а не перед всем заголовком: номер попадает на страницу ДО
% разрыва (осиротевшим внизу предыдущей страницы или один на пустой
% странице), а название - уже после разрыва, без номера. \pretocmd
% (etoolbox) вместо этого пришивает \clearpage перед самим \section
% целиком, до того как titlesec вообще начинает строить заголовок -
% разрыв происходит раньше, чем номер и название вообще собираются.
\usepackage{etoolbox}
% \nohyphens -- п. 4.2: "Переносы слов в заголовках не допускаются" (в основном
% тексте перенос лишь сильно штрафуется, см. блок про переносы выше, а в
% заголовках запрещён полностью: штраф 10000 TeX трактует как запрет точки
% переноса).
\newcommand{\nohyphens}{\hyphenpenalty=10000\exhyphenpenalty=10000\relax}
% У заголовков разделов шрифт полужирный (п. 4.2), у подразделов -- обычный
% (п. 4.4).
\titleformat{\section}{\bfseries\raggedright\nohyphens}{\thesection}{0.5em}{}
\pretocmd{\section}{\clearpage}{}{}
\titleformat{\subsection}{\raggedright\nohyphens}{\thesubsection}{0.5em}{}
\titleformat{\subsubsection}{\raggedright\nohyphens}{\thesubsubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{2\baselineskip}{1\baselineskip}
\titlespacing*{\subsection}{0pt}{1\baselineskip}{1\baselineskip}
\titlespacing*{\subsubsection}{0pt}{1\baselineskip}{1\baselineskip}
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% ---------- Формулы (метод экспертных оценок, раздел 5) ----------
\usepackage{amsmath}
\usepackage{amssymb}

% ---------- Списки ----------
\usepackage{enumitem}
% п. 3.6 пособия допускает перед перечислением дефис либо строчную букву со
% скобкой, а арабские цифры со скобкой -- для дальнейшей детализации. По
% решению автора нумерованные перечисления везде набираются арабскими цифрами
% со скобкой (как было изначально), маркированные -- тире.
\setlist[itemize]  {noitemsep, topsep=0pt, parsep=0pt, leftmargin=0pt, itemindent=2.5cm, labelsep=1cm, label=--}
\setlist[enumerate,1]{itemsep=0pt, topsep=0pt, parsep=0pt, leftmargin=0pt, itemindent=2.7cm, labelsep=1cm, label=\arabic*)}
% Отдельный список для библиографического списка (п. 14.2) -- та же арабская
% нумерация, объявлен отдельно, чтобы её нельзя было случайно переопределить
% вместе с обычными перечислениями.
\newlist{numlist}{enumerate}{1}
\setlist[numlist]{itemsep=0pt, topsep=0pt, parsep=0pt, leftmargin=0pt, itemindent=2.7cm, labelsep=1cm, label=\arabic*)}

% ---------- Блоки кода/команд ----------
% См. подробное объяснение в admin_manual/config.tex - тот же приём:
% перенос длинных строк вместо ухода за поле страницы.
\usepackage{fvextra}
\DefineVerbatimEnvironment{verbatim}{Verbatim}{breaklines=true, breakanywhere=true, fontsize=\small}

% ---------- Таблицы ----------
\usepackage{graphicx}
\usepackage{float}
\usepackage{rotating}   % повёрнутые на всю страницу рисунки: sidewaysfigure поворачивает рисунок и подпись как единый блок, а колонтитул (номер страницы) остаётся на обычном месте внизу страницы -- под повёрнутым рисунком
\usepackage{array}
\usepackage{xltabular}   % longtable + X-колонки: таблицы переносятся между страницами
% ---------- ГОСТ 2.105: разрыв таблицы между страницами ----------
% Требование: при переносе таблицы на следующую страницу нижнюю
% горизонтальную линию, ограничивающую таблицу, не проводят.
% longtable (основа xltabular) рисует каждый \hline как ДВЕ линейки подряд,
% разделённые \LT@sep = «штраф разрыва + \vskip-\arrayrulewidth». Если
% разрыва нет, отрицательный клей совмещает их и видна одна линия; если
% разрыв есть, первая линейка остаётся внизу страницы (та самая лишняя
% нижняя кромка), вторая уходит на начало следующей. Патч делает первую
% линейку нулевой высоты (без чернил) и убирает компенсирующий клей:
% суммарная высота и положение всех линий не меняются, но в месте переноса
% внизу страницы линии больше нет. Проверено попиксельно: позиции всех
% остальных линеек совпадают с непропатченным longtable.
\makeatletter
\ExplSyntaxOn
\cs_if_exist:NT \tbl_gdecr_row_count:
  {
    \def\LT@@hline{%
      \ifx\@let@token\hline
        \global\let\@gtempa\@gobble
        \gdef\LT@sep{\penalty-\@medpenalty\vskip\doublerulesep}%
      \else
        \global\let\@gtempa\@empty
        \gdef\LT@sep{\penalty-\@lowpenalty}%
      \fi
      \ifnum0=`{\fi}%
      \multispan\LT@cols\unskip\leaders\hrule\@height\z@\hfill\cr
      \noalign{\tbl_gdecr_row_count:\LT@sep}%
      \multispan\LT@cols\unskip\leaders\hrule\@height\arrayrulewidth\hfill\cr
      \noalign{\tbl_gdecr_row_count:\penalty\@M}%
      \@gtempa}%
  }
\ExplSyntaxOff
% Разрыв нельзя делать сразу ПОСЛЕ линейки \cline -- иначе она остаётся
% внизу страницы. Запрещаем разрыв в этом месте: TeX выбирает предыдущую
% допустимую точку (границу перед строкой-линейкой), и линейка уходит на
% следующую страницу вместе с продолжением таблицы. \nobreak обязан стоять
% ДО отрицательного клея, иначе разрыв всё равно возможен на самом клее,
% поэтому \patchcmd не годится -- определение \@cline повторено целиком
% (array.sty) с изменённой последней строкой.
\ExplSyntaxOn
\cs_if_exist:NT \tbl_gdecr_row_count:
  {
    \def\@cline#1-#2\@nil{
      \omit
      \@multicnt#1
      \advance\@multispan\m@ne
      \ifnum\@multicnt=\@ne\@firstofone{&\omit}\fi
      \@multicnt#2
      \advance\@multicnt-#1
      \advance\@multispan\@ne
      \UseTaggingSocket{tbl/leaders/begin}
      \leaders\hrule\@height\arrayrulewidth\hfill
      \UseTaggingSocket{tbl/leaders/end}
      \tbl_gdecr_row_count:
      \cr
      \noalign{\nobreak\vskip-\arrayrulewidth\nobreak}
    }
  }
\ExplSyntaxOff
\makeatother

\usepackage{seqsplit}    % перенос длинных идентификаторов без дефиса, когда они не влезают в колонку
\usepackage{caption}
\captionsetup[table]{position=top, labelsep=endash, name=Таблица, justification=raggedright, singlelinecheck=false}
\captionsetup[figure]{position=bottom, labelsep=endash, name=Рисунок, justification=centering}
\renewcommand{\arraystretch}{1.15}

% ---------- Узкие таблицы: подпись по левому краю самой таблицы ----------
% \caption (justification=raggedright выше) выравнивается по левому краю
% \linewidth, т.е. по полю страницы -- для таблицы уже textwidth это
% совпадает с левым краем таблицы, но для более узкой центрированной
% таблицы (тройка "Критерии и веса...", "Итоговые результаты...") подпись
% съезжает левее самой таблицы. \gosttablebox измеряет фактическую ширину
% таблицы (\lrbox), после чего подпись и таблица кладутся в minipage той
% же ширины -- у обеих общий левый край, а minipage целиком центрируется
% на странице тем же \centering, что раньше центрировал только таблицу.
\newsavebox{\gosttablebox}
\newenvironment{gosttable}[1]{%
  \def\gosttablecaption{#1}%
  \begin{lrbox}{\gosttablebox}%
}{%
  \end{lrbox}%
  \begin{minipage}{\wd\gosttablebox}
  \caption{\gosttablecaption}
  \usebox{\gosttablebox}
  \end{minipage}%
}

% ---------- Схемы (архитектура, ER-модель, диаграммы последовательностей) ----------
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, shapes.multipart, fit, backgrounds, calc}

% Три слоя вместо стандартных двух (background/main): лайнлайн участника
% рисуется на seqlifelayer -- позади всего, включая background, -- поэтому
% полоска активации (seqact, слой background, непрозрачная заливка) реально
% перекрывает и прячет дашед-лайнлайн под собой, как в example.pdf, а не
% просто "почти совпадает" с ним. Сам background по-прежнему нужен отдельно
% от main -- иначе полоска активации рисуется поверх подписи условия alt,
% когда та выравнивается по x с лайнлайном участника.
\pgfdeclarelayer{seqlifelayer}
\pgfsetlayers{seqlifelayer,background,main}

% ---------- Диаграммы последовательностей (UML sequence, Приложение А) ----------
% Тот же макро-набор, что и в tech_zadanie/config.tex (Приложение Б) -- курсор
% \seqcur сам отступает вниз на нужную величину после каждой команды, поэтому
% y-координаты не считаются вручную. anchor=south/north (а не центрирование
% на (#1,0)) -- при многострочной подписи участника бокс растёт от центра в
% обе стороны, и фиксированный отступ попадал бы внутрь бокса.
\tikzset{
  seqactor/.style={draw, rectangle, minimum width=2.9cm, minimum height=0.9cm, text width=2.6cm, align=center, font=\small, fill=white},
  seqlife/.style={dashed, gray!60!black},
  seqact/.style={draw, fill=white},
  seqmsg/.style={-{Stealth}, font=\scriptsize, align=center},
  seqret/.style={-{Stealth}, dashed, font=\scriptsize, align=center},
}
\newlength{\seqcur}
\newcommand{\seqinit}{\setlength{\seqcur}{-0.45cm}}
\newcommand{\seqhead}[2]{\node[seqactor, anchor=south] at (#1,0) {#2};}
\newcommand{\seqfoot}[3]{%
  \node[seqactor, anchor=north] at (#1,#3) {#2};
  \begin{pgfonlayer}{seqlifelayer}\draw[seqlife] (#1,0) -- (#1,#3);\end{pgfonlayer}
}
\newcommand{\seqpad}[1]{\addtolength{\seqcur}{-#1cm}}
\newcommand{\seqmark}[1]{\pgfmathsetmacro{#1}{\seqcur/1cm}}
% Оба конца стрелки сдвинуты на 0.09cm друг к другу -- ровно на половину
% ширины полоски активации (см. \seqact), так что стрелка упирается в
% боковую грань бокса участника, а не в сам лайнлайн, независимо от
% направления сообщения.
\newcommand{\seqmsgA}[3]{\seqpad{0.75}\seqmark{\seqy}\draw[seqmsg] ($(#1,\seqy)!0.09cm!(#2,\seqy)$) -- ($(#2,\seqy)!0.09cm!(#1,\seqy)$) node[midway, above] {#3};}
\newcommand{\seqretA}[3]{\seqpad{0.75}\seqmark{\seqy}\draw[seqret] ($(#1,\seqy)!0.09cm!(#2,\seqy)$) -- ($(#2,\seqy)!0.09cm!(#1,\seqy)$) node[midway, above] {#3};}
\newcommand{\seqmsgB}[3]{\seqpad{1.1}\seqmark{\seqy}\draw[seqmsg] ($(#1,\seqy)!0.09cm!(#2,\seqy)$) -- ($(#2,\seqy)!0.09cm!(#1,\seqy)$) node[midway, above] {#3};}
\newcommand{\seqretB}[3]{\seqpad{1.1}\seqmark{\seqy}\draw[seqret] ($(#1,\seqy)!0.09cm!(#2,\seqy)$) -- ($(#2,\seqy)!0.09cm!(#1,\seqy)$) node[midway, above] {#3};}
\newcommand{\seqcond}[2]{\seqpad{0.55}\seqmark{\seqy}\node[font=\scriptsize\itshape, align=center] at (#1,\seqy) {[#2]};}
\newcommand{\seqselfA}[2]{%
  \seqpad{0.3}\seqmark{\seqy}
  \draw[seqmsg] (#1+0.09,\seqy) -- ++(0.8,0) -- ++(0,-0.4) -- (#1+0.09,\seqy-0.4);
  \node[font=\scriptsize, align=left, anchor=north west, inner sep=0pt] at (#1+0.95,\seqy-0.4) {#2};
  \seqpad{0.75}
}
\newcommand{\seqselfB}[2]{%
  \seqpad{0.3}\seqmark{\seqy}
  \draw[seqmsg] (#1+0.09,\seqy) -- ++(0.8,0) -- ++(0,-0.4) -- (#1+0.09,\seqy-0.4);
  \node[font=\scriptsize, align=left, anchor=north west, inner sep=0pt] at (#1+0.95,\seqy-0.4) {#2};
  \seqpad{1.1}
}
% На фоновом слое -- иначе полоска рисуется поверх подписи условия alt,
% когда та выравнивается по x с лайнлайном участника.
\newcommand{\seqact}[3]{\begin{pgfonlayer}{background}\draw[seqact] (#1-0.09,#2) rectangle (#1+0.09,#3);\end{pgfonlayer}}
\newcommand{\seqframe}[5]{%
  \draw (#2,#4) rectangle (#3,#5);
  \node[anchor=north west, font=\scriptsize\bfseries, inner sep=2pt] at (#2,#4) {#1};
}
\newcommand{\seqaltframe}[4]{\seqframe{alt}{#1}{#2}{#3}{#4}}
\newcommand{\seqloopframe}[4]{\seqframe{loop}{#1}{#2}{#3}{#4}}
\newcommand{\seqsep}[3]{\draw[seqlife] (#1,#2) -- (#3,#2);}

% ---------- Колонтитулы ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% ---------- Оглавление: точки-заполнители до номера страницы у разделов ----------
% По умолчанию в article.cls у \l@section (в отличие от \l@subsection) нет
% отточия -- копия стандартного определения с добавленным \leaders, как у
% \l@subsection.
\makeatletter
\renewcommand*\l@section[2]{%
  \ifnum \c@tocdepth >\z@
    \addpenalty\@secpenalty
    % 0.4em вместо стандартного 1.0em: после добавления подраздела 5.1 и
    % приложения А содержание перестало помещаться на один лист, а отбивка
    % перед записями разделов -- единственный запас по вертикали.
    \addvspace{0.4em \@plus\p@}%
    \setlength\@tempdima{1.5em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak
      \leaders\hbox{$\m@th\mkern \@dotsep mu.\mkern\@dotsep mu$}\hfill
      \nobreak\hb@xt@\@pnumwidth{\hss #2}\par
      \penalty\@highpenalty
    \endgroup
  \fi}
\makeatother

% ---------- Гиперссылки и оглавление ----------
\usepackage[unicode, hidelinks, bookmarksdepth=3]{hyperref}
\usepackage{xurl}   % длинные URL переносятся по /, ., а не вылезают за поле
\usepackage{bookmark}
\hypersetup{
  pdftitle={Пояснительная записка},
  pdfauthor={Решетняк Владислав Александрович},
  pdflang={ru}
}

% ---------- Колонки для справочных таблиц ----------
% \nohyphens в текстовых графах: в узкой колонке перенос особенно заметен, а
% п. 2.12 переносы не рекомендует; \raggedright снимает переполнение, которое
% иначе возникло бы без переносов.
\newcolumntype{M}{>{\raggedright\arraybackslash\nohyphens}p{5.7cm}}   % название/термин
\newcolumntype{D}{>{\raggedright\arraybackslash\hangindent=0pt\nohyphens}X}  % описание
\newcolumntype{T}{>{\raggedright\arraybackslash\nohyphens}p{6.3cm}}   % критерий (таблицы 1-3)

% ---------- Заголовок приложения (пп. 12.3, 12.5, 12.6) ----------
% п. 12.3: приложение начинается с нового листа, слово "Приложение" и его
% обозначение -- наверху посередине страницы; п. 12.6: заголовок приложения --
% отдельной строкой, симметрично тексту. Строка с характером приложения
% ("(справочное)", п. 12.5) по решению автора не печатается.
% #1 -- буква, #2 -- заголовок.
\newcommand{\gostappendix}[2]{%
  \clearpage
  \addcontentsline{toc}{section}{ПРИЛОЖЕНИЕ #1. #2}%
  \begin{center}
    {\bfseries ПРИЛОЖЕНИЕ #1}\\[\baselineskip]
    {\bfseries #2}
  \end{center}
  \par\vspace{0.5\baselineskip}
}

% ---------- Длинные идентификаторы (переменные окружения, поля БД) ----------
\newcommand{\envvar}[1]{\texttt{\seqsplit{#1}}}

% ---------- Разделы с ручной нумерацией по ГОСТ (см. admin_manual/config.tex) ----------
\newcommand{\gostsection}[2]{\section*{\hspace*{\gostindent}#1\quad #2}\addcontentsline{toc}{section}{#1\quad #2}}
\newcommand{\gostsubsection}[2]{\subsection*{\hspace*{\gostindent}#1\quad #2}\addcontentsline{toc}{subsection}{#1\quad #2}}
\newcommand{\gostsubsubsection}[2]{\subsubsection*{\hspace*{\gostindent}#1\quad #2}\addcontentsline{toc}{subsubsection}{#1\quad #2}}

% ---------- Литеральное подчёркивание в идентификаторах (SENTRY_DSN и т.п.) ----------
% Загружается САМОЙ ПОСЛЕДНЕЙ в преамбуле, после tikz/fvextra и всех
% остальных пакетов: опция [strings] делает "_" активным символом сразу
% при загрузке, а внутренние \csname...\endcsname-конструкции у fancyvrb
% (ключ file_ext) и у pgf (стандартная стрелка с именем "_" в
% pgfcorearrows.code.tex) ломаются, если "_" уже активен в момент их
% чтения. На типографику основного текста (main.tex) порядок загрузки не
% влияет -- активация действует на весь документ независимо от того, где
% в преамбуле она произошла, лишь бы раньше \begin{document}.
\usepackage[strings]{underscore}
