% Сборка технического задания для вкладки приложением Б в пояснительную
% записку (zapiska/main.tex). Содержимое то же, что у main.tex, — отличается
% только нумерация страниц: она продолжает записку, а не начинается заново.
%
% Раскладка комплекта: записка занимает страницы 1--58, титульный лист ТЗ --
% страницу 59 (номер на нём не печатается, как и на титульном листе записки;
% обозначение «ПРИЛОЖЕНИЕ Б» стоит на этом же листе, отдельного
% листа-разделителя нет), поэтому содержание ТЗ -- уже 60-я.
%
% \def до \documentclass допустимо: это определение, а не команда LaTeX,
% требующая уже загруженного класса. main.tex подхватывает его через
% \providecommand, поэтому самостоятельная сборка main.tex не меняется.
%
% Собирать: latexmk -xelatex main-appendix.tex (в каталоге tech_zadanie).
% После изменения объёма записки или ТЗ номер ниже нужно пересчитать, иначе
% сквозная нумерация разъедется — это единственное место, где он задан.
% Обозначение приложения печатается на титульном листе ТЗ (titlepage.tex);
% в самостоятельной сборке макрос не определён и блок не выводится.
\def\tzappendixletter{Б}
\def\firstpagenumber{60}
\input{main}
