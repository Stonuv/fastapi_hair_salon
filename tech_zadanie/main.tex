% main.tex -- основной файл документа. Оформление и пакеты -- в config.tex.
\input{config}

\begin{document}

% ============================================================
% ТИТУЛЬНЫЙ ЛИСТ -- отдельный файл titlepage.tex
% ============================================================
\input{titlepage}
\setcounter{page}{2}

\newpage
\renewcommand{\contentsname}{СОДЕРЖАНИЕ}
\begingroup
\titleformat{\section}{\bfseries\centering}{\thesection}{0.5em}{}
\tableofcontents
\endgroup
\newpage

% ============================================================
% Список сокращений и список терминов -- вспомогательные перечни,
% не являющиеся разделами документа: \section* без \addcontentsline,
% поэтому в содержании они не указываются (как в пояснительной записке).
% ============================================================
\section*{\hspace*{\gostindent}СПИСОК СОКРАЩЕНИЙ}

\begin{itemize}[leftmargin=0pt, itemindent=1.5cm, labelsep=0pt, label={}]
\item \textbf{API} -- Application Programming Interface -- программный интерфейс взаимодействия компонентов системы.
\item \textbf{CMS} -- Content Management System -- система управления содержимым сайта.
\item \textbf{CPU} -- Central Processing Unit -- центральный процессор.
\item \textbf{CRM} -- Customer Relationship Management -- управление взаимоотношениями с клиентами.
\item \textbf{CSRF} -- Cross-Site Request Forgery -- межсайтовая подделка запроса.
\item \textbf{CSS} -- Cascading Style Sheets -- язык описания внешнего вида веб-страницы.
\item \textbf{ER} -- Entity--Relationship -- модель «сущность--связь», используемая для инфологического описания данных.
\item \textbf{FK} -- Foreign Key -- внешний ключ реляционной таблицы.
\item \textbf{GB} -- гигабайт.
\item \textbf{HTTP} -- HyperText Transfer Protocol -- протокол передачи данных прикладного уровня; \textbf{HTTPS} -- тот же протокол поверх шифрованного канала TLS.
\item \textbf{ID} -- идентификатор объекта или учётной записи.
\item \textbf{JSON} -- JavaScript Object Notation -- текстовый формат обмена данными.
\item \textbf{JWT} -- JSON Web Token -- формат токена авторизации, используемый для access-токена сессии.
\item \textbf{MVP} -- Minimum Viable Product -- минимально жизнеспособный продукт; объём функций первой очереди системы.
\item \textbf{OAuth} -- открытый протокол делегированной авторизации через внешнего поставщика учётных записей.
\item \textbf{ORM} -- Object-Relational Mapping -- отображение объектов языка программирования на строки реляционной таблицы.
\item \textbf{PKCE} -- Proof Key for Code Exchange -- расширение OAuth, защищающее обмен кода авторизации на токен от перехвата.
\item \textbf{RAM} -- Random Access Memory -- оперативная память.
\item \textbf{RBAC} -- Role-Based Access Control -- разграничение доступа на основе ролей.
\item \textbf{REST} -- Representational State Transfer -- архитектурный стиль построения API поверх HTTP.
\item \textbf{SEO} -- Search Engine Optimization -- оптимизация страниц под поисковые системы.
\item \textbf{SHA} -- Secure Hash Algorithm -- семейство криптографических хеш-функций (в системе -- SHA-256).
\item \textbf{SMS} -- Short Message Service -- служба коротких текстовых сообщений.
\item \textbf{SMTP} -- Simple Mail Transfer Protocol -- протокол отправки электронной почты.
\item \textbf{SQL} -- Structured Query Language -- язык запросов к реляционной СУБД.
\item \textbf{SSD} -- Solid State Drive -- твердотельный накопитель.
\item \textbf{TLS} -- Transport Layer Security -- протокол шифрования канала связи (HTTPS).
\item \textbf{URL} -- Uniform Resource Locator -- адрес ресурса в сети.
\item \textbf{UTF-8} -- Unicode Transformation Format, 8-bit -- кодировка символов, используемая в системе.
\item \textbf{UUID} -- Universally Unique Identifier -- универсальный уникальный идентификатор.
\item \textbf{VK ID} -- сервис единой учётной записи «ВКонтакте», используемый как внешний поставщик авторизации.
\item \textbf{VPS} -- Virtual Private Server -- виртуальный выделенный сервер.
\item \textbf{XSS} -- Cross-Site Scripting -- внедрение стороннего сценария в страницу приложения.
\item \textbf{БД} -- база данных.
\item \textbf{ПО} -- программное обеспечение.
\item \textbf{СУБД} -- система управления базами данных (в проекте -- PostgreSQL 16).
\item \textbf{ТЗ} -- техническое задание.
\item \textbf{ФЗ} -- федеральный закон.
\item \textbf{ФТ} -- функциональное требование; карточки ФТ-001--ФТ-016 приведены в подразделе 4.2 настоящего ТЗ.
\end{itemize}

\section*{\hspace*{\gostindent}СПИСОК ТЕРМИНОВ}

\begin{itemize}[leftmargin=0pt, itemindent=1.5cm, labelsep=0pt, label={}]
\item \textbf{Владелец сети} -- пользователь с наибольшим объёмом прав: управляет всеми точками сети, каталогом услуг, содержимым сайта и назначением администраторов точек.
\item \textbf{Запись} -- забронированный клиентом интервал времени у конкретного мастера на конкретную услугу в конкретной точке сети.
\item \textbf{Карточка функционального требования} -- таблица единого формата, описывающая одну функцию системы: наименование, роли, входные и выходные данные, основной сценарий, альтернативные сценарии и обработку ошибок.
\item \textbf{Мастер} -- сотрудник точки, оказывающий услуги клиентам; в системе -- роль пользователя.
\item \textbf{Мягкое удаление} -- перевод строки в неактивное состояние (поле \texttt{deleted\_at} или \texttt{is\_active}) вместо физического удаления, сохраняющий историю операций.
\item \textbf{Слот} -- свободный интервал в расписании мастера, доступный для записи клиента.
\item \textbf{Точка сети} -- отдельная парикмахерская или барбершоп в составе сети одного владельца -- собственный адрес, часы работы, мастера и записи.
\end{itemize}

% ============================================================
\section{Общие сведения}
% ============================================================

\subsection{Полное наименование системы и приложения}

Наименование: CMS для парикмахерских и барбершопов.

Приложение: «Сайтама» (веб-приложение с клиентской и административной частью).

Понятия «парикмахерская» и «барбершоп» используются в настоящем ТЗ как взаимозаменяемые синонимы -- предприятие сферы услуг стрижки и ухода за волосами/бородой (см. также раздел 3.1). Термин «груминг-салон» в наименование системы не включён: в русскоязычной практике он обозначает услуги стрижки и ухода за животными, что не относится к сфере деятельности системы.

Система рассчитана на сеть из одной или нескольких точек обслуживания одного владельца: у сети общий каталог услуг и публичный сайт, но у каждой точки -- собственный адрес, часы работы, мастера и записи (см. 3.1, 5.1.1 -- сущность «Точка сети», и 5.1.2).

\subsection{Наименования заказчика и разработчика}

Заказчик: Решетняк Владислав Александрович, 335б, 3 курс.

Разработчик: Решетняк Владислав Александрович, 335б, 3 курс.

Руководитель проекта: Оганезова Нина Александровна.

\subsection{Сроки разработки}

Начало работ: 22.06.2026.

Окончание работ: 18.07.2026.

\subsection{Перечень документов, на которые ссылается ТЗ}

\begin{itemize}
  \item ГОСТ 34.602-2020 «Информационная технология. Комплекс стандартов на автоматизированные системы. Техническое задание на создание автоматизированной системы»;
  \item ГОСТ 34.601-90 «Информационная технология. Комплекс стандартов на автоматизированные системы. Автоматизированные системы. Стадии создания»;
  \item ГОСТ 19.201-78 «Единая система программной документации. Техническое задание. Требования к содержанию и оформлению»;
  \item Федеральный закон № 152-ФЗ «О персональных данных»;
  \item Федеральный закон № 149-ФЗ «Об информации, информационных технологиях и о защите информации».
\end{itemize}



% ============================================================
\section{Назначение и цели создания системы}
% ============================================================

\subsection{Назначение системы}

Система предназначена для автоматизации операционной деятельности барбершопов и парикмахерских, в том числе организованных в сеть из нескольких точек обслуживания одного владельца: ведения клиентской базы, управления расписанием мастеров, онлайн-записи клиентов, управления общим для сети каталогом услуг, формирования отчётов и ценообразованием, а также автоматической отправки писем-напоминаний о записи. Система обеспечивает кроссплатформенный доступ через веб-браузер, разграничивает права администратора точки (видит и обслуживает только свою точку) и владельца сети (видит и управляет всеми точками) и предоставляет оперативную аналитику по выручке, загруженности мастеров и популярности услуг -- как по отдельной точке, так и по сети в целом.

Система автоматизирует следующие бизнес-процессы, выделенные в ходе исследования:
\begin{enumerate}
  \item онлайн-запись клиента (Зап-1);
  \item управление услугами и прайсом (Усл-2);
  \item напоминания о записи (Увед-3);
  \item формирование отчётов (Отчёт-4);
  \item управление контентом сайта -- настройки, отображаемые на главной странице и странице мастеров (Конт-5);
  \item управление сетью точек обслуживания -- открытие, закрытие, изменение адреса, телефона и часов работы каждой точки (Точ-6).
\end{enumerate}

\subsection{Цели создания}

Каждая цель сформулирована с измеримым, проверяемым на приёмке показателем.

\begin{itemize}
  \item \textbf{Сокращение времени обработки заявок.} Перевод записи в самообслуживание клиента: для уже зарегистрированного клиента время от открытия страницы записи до подтверждённой записи -- не более 2 минут (против оценочных 5--10 минут при ручной обработке администратором по телефону); регистрация нового клиента (однократная операция) в этот показатель не входит.
  \item \textbf{Устранение накладок и двойных записей.} 0 инцидентов двойного бронирования одного временного слота одного мастера -- гарантируется ограничением целостности PostgreSQL (EXCLUDE USING gist), а не организационными мерами; проверяется нагрузочным тестированием конкурентных запросов (раздел 7.1).
  \item \textbf{Снижение доли пропущенных визитов.} Автоматические напоминания за 24 и 2 часа до визита (см. ФТ-008) призваны снизить долю визитов, забытых клиентом. Система не ведёт отдельного статуса «неявка» -- факт оказания услуги мастер фиксирует вручную статусом \texttt{done} (см. 5.1.1), поэтому проверяемым на приёмке показателем служит технический: не менее 95\% подтверждённых записей получают оба напоминания (заполнены \texttt{reminder\_24h\_sent\_at} и \texttt{reminder\_2h\_sent\_at}) до наступления времени визита.
  \item \textbf{Гибкое ценообразование.} Возможность настроить индивидуальную стоимость услуг у конкретного мастера: коэффициентом мастера, умножающим базовую цену каждой оказываемой им услуги, либо точечным переопределением цены для одной услуги у одного мастера (см. ФТ-007). Формулировка уточнена: система не вычисляет цену автоматически по квалификации мастера -- специализация мастера отображается администратору как справочная информация, но не участвует в расчёте.
  \item \textbf{Единое хранилище данных.} Полный отказ от бумажных журналов и разрозненных Excel-таблиц к моменту сдачи системы (100\% операций записи, каталога услуг и клиентской базы ведётся в системе).
  \item \textbf{Аналитика для владельца.} Формирование отчёта по выручке, загруженности и топ-услугам за произвольный период -- без привлечения разработчика, время формирования отчёта не более 3 секунд при объёме данных до 10~000 записей.
  \item \textbf{Упрощение авторизации клиентов.} Не менее 30\% новых клиентов используют вход через VK ID вместо email-регистрации в течение первого месяца эксплуатации.
\end{itemize}

% ============================================================
\section{Характеристики объекта автоматизации}
% ============================================================

\subsection{Описание предметной области}

Объектом автоматизации является барбершоп, оказывающий услуги мужской стрижки, оформления бороды и сопутствующего ухода, в том числе организованный в сеть из нескольких точек обслуживания одного владельца. Пилотным объектом внедрения выступает барбершоп «Сайтама»; система проектируется технологически нейтрально к конкретному предприятию сферы услуг красоты и может быть использована другой парикмахерской или барбершопом без изменения кода -- заменой каталога услуг и контента публичных страниц. Каждая точка сети имеет собственный адрес, часы работы и мастеров; каталог услуг и оформление публичного сайта -- общие на всю сеть (см. 5.1.1, сущность «Точка сети»).

Предметная область охватывает:
\begin{itemize}
  \item фиксацию и обработку заявок клиентов на запись к мастеру на конкретную услугу и время;
  \item ведение недельного расписания работы каждого мастера по дням недели, в пределах часов работы точки, к которой он относится (см. ФТ-011);
  \item управление каталогом услуг с индивидуальными ценами для каждого мастера;
  \item ведение клиентской базы (контактные данные, история визитов, способ авторизации); учётная запись клиента общая на всю сеть -- повторная регистрация в каждой точке не требуется;
  \item автоматическую отправку писем-напоминаний о записи (за 24 и 2 часа до визита) и уведомления об изменении времени записи мастером/администратором;
  \item расчёт свободных временных слотов мастера на выбранную дату с учётом уже существующих записей (без технологического буфера между соседними записями, см. ФТ-004);
  \item оформление записи клиентом (доступно только вошедшим в систему пользователям, см. ФТ-005);
  \item приём отзывов клиентов о мастерах после завершённой услуги (см. ФТ-014);
  \item управление контентом публичных страниц сайта (главная страница, страница мастеров, личный кабинет клиента);
  \item управление сетью точек обслуживания -- открытие, закрытие, изменение адреса, телефона и часов работы каждой точки;
  \item формирование отчётов, включая аналитику загруженности мастеров, по отдельной точке и по сети в целом (см. ФТ-015);
  \item разграничение прав доступа между ролями: клиент, мастер, администратор точки, владелец сети.
\end{itemize}

\subsection{Границы автоматизации}

Вне границ автоматизации настоящей системы находятся следующие процессы -- барбершоп продолжает вести их вне системы или с помощью сторонних инструментов:
\begin{itemize}
  \item бухгалтерский и налоговый учёт (интеграция с 1С или иными бухгалтерскими системами не выполняется);
  \item складской учёт расходных материалов и косметических средств;
  \item расчёт и начисление заработной платы мастерам -- система не ведёт кадровый учёт; настраиваемый у мастера коэффициент цены (\texttt{coefficient}, см. 5.1.1) используется только для расчёта стоимости услуг клиенту, а не вознаграждения мастера;
  \item приём онлайн-оплаты и возврат денежных средств -- оплата услуг производится очно в барбершопе, система не выступает платёжным агрегатором (см. также ФТ-006);
  \item учёт фактической явки клиента отдельным статусом записи -- терминальные статусы записи ограничены значениями «завершена» (услуга оказана, отмечается мастером) и «отменена» (см. 5.1.1); отдельного статуса «неявка» система не ведёт;
  \item CRM-функции за пределами базового профиля клиента: сегментация базы, email/SMS-маркетинговые рассылки, программы лояльности -- не входят в MVP.
\end{itemize}

\subsection{Особенности реализации веб-приложения}

\begin{itemize}
  \item централизованное хранение данных на сервере в PostgreSQL;
  \item многопользовательский доступ с разграничением прав по ролям (RBAC), включая ограничение видимости данных точкой сети для администратора;
  \item FastAPI отдаёт REST API, потребляемое Vue.js-фронтендом;
  \item интеграция с внешним провайдером идентификации (VK ID);
  \item отправка писем напрямую через SMTP, без стороннего провайдера рассылок и без SMS-канала.
\end{itemize}

% ============================================================
\section{Требования к системе}
% ============================================================

\subsection{Требования к системе в целом}

\subsubsection{Требования к структуре и функционированию системы}

Клиент-серверная архитектура:

\begin{xltabular}{\textwidth}{|l|X|}
\caption{Компонентный состав системы}\\
\hline
\textbf{Компонент} & \textbf{Технология} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Компонент} & \textbf{Технология} \\
\hline
\endhead
\endfoot
Frontend & Vue.js + Tailwind CSS \\\hline
Backend & FastAPI (Python) \\\hline
СУБД & PostgreSQL 16+ \\\hline
ORM & SQLAlchemy 2.0 (синхронный) + Alembic (миграции) \\\hline
Валидация данных & Pydantic \\\hline
Напоминания о записи & фоновый asyncio-цикл в процессе backend (без выделенного воркера и брокера сообщений), см. ФТ-008 \\\hline
\end{xltabular}

Система функционирует как веб-приложение, доступное через браузер по протоколу HTTPS, с обязательным интернет-соединением. Взаимодействие фронтенда и бэкенда осуществляется через REST API.

\subsubsection{Требования к численности и квалификации персонала}

Администратор системы -- базовые навыки работы с административной панелью, специальной ИТ-подготовки не требуется.

Мастера и клиенты -- не требуют специальной подготовки, интерфейс интуитивно понятен.

Обслуживание сервера (обновления, резервное копирование) -- силами разработчика или на аутсорсе, специальной штатной единицы не предусмотрено.

\subsubsection{Требования к надёжности}

Типовое для промышленных систем значение среднего времени наработки на отказ (8760 часов, 1 год) не подкрепляется методикой расчёта, так как не может быть проверено в отведённые на практику сроки. Установлены следующие требования:

\begin{itemize}
  \item среднее время наработки на отказ -- не менее 200 часов непрерывной работы в период опытной эксплуатации (соответствует периоду от развёртывания до защиты проекта, раздел 6, этап 5);
  \item время восстановления после сбоя -- не более 30 минут;
  \item обязательное автоматическое резервное копирование базы данных не реже одного раза в сутки, хранение резервных копий не менее 7 дней;
  \item защита от потери данных при одновременной записи двух клиентов на один слот обеспечивается ограничением целостности PostgreSQL (EXCLUDE USING gist).
\end{itemize}

\textbf{Критерии отказа.} Отказом системы считается любое из следующих событий: недоступность системы (HTTP-ошибки 5xx на основных эндпоинтах либо отсутствие ответа) продолжительностью более 5 минут; потеря или необратимое искажение данных о записи или клиенте; невозможность выполнить любую из функций ФТ-001--ФТ-016 в течение более 10 минут подряд для более чем одного пользователя одновременно.

\subsubsection{Требования к безопасности}

\begin{itemize}
  \item аутентификация пользователей: логин/пароль (email), а также OAuth 2.1 (PKCE) через VK ID;
  \item хранение паролей -- только в виде хэша (bcrypt), хранение паролей в открытом виде запрещено;
  \item разграничение прав доступа по ролям: клиент, мастер, администратор точки, владелец сети; администратору дополнительно ограничена видимость данных его точкой (см. 3.1);
  \item авторизация запросов к API -- JWT access-токен, 15 минут; сессия входа (refresh) хранится на сервере как запись в таблице сессий с хешем токена в httpOnly-cookie -- действительна 30 дней бездействия; смена/сброс пароля и блокировка пользователя аннулируют все его активные сессии;
  \item учёт попыток входа (успешных и неудачных, с IP-адресом и меткой времени) для защиты от подбора пароля -- см. сущность «Попытка входа», 5.1.1; пять неудачных попыток по одному email за 15 минут временно блокируют вход по паролю для этого адреса;
  \item ограничение частоты запросов (rate limiting) по IP для функций, наиболее чувствительных к злоупотреблению: регистрация -- не более 5 запросов в час, создание записи (онлайн-запись) -- не более 20 запросов в минуту, оставление отзыва -- не более 10 запросов в час; превышение лимита возвращает ошибку 429;
  \item персональные данные клиентов (ФИО, телефон, email) обрабатываются в соответствии с ФЗ-152;
  \item защита от CSRF/XSS/SQL-инъекций стандартными средствами FastAPI и SQLAlchemy (параметризованные запросы, экранирование).
\end{itemize}

\textbf{Определение способа входа.} Отдельного поля-перечисления в модели пользователя нет: способ входа определяется по факту заполненности полей -- наличие хеша пароля разрешает вход по email/паролю, наличие идентификатора VK ID -- через VK ID (см. 5.1.1); поля независимы и могут быть заполнены одновременно. Первый вход через VK ID, чей email совпадает с email уже существующей учётной записи (созданной по паролю), автоматически привязывает VK ID к этой записи, а не создаёт вторую -- подробнее см. ФТ-003.

\subsubsection{Требования к эргономике и технической эстетике}

Адаптивный дизайн, поскольку большая часть клиентов записывается с мобильных устройств.

Время отклика интерфейса -- не более 300 мс на типовых операциях при штатной нагрузке (без учёта сетевых задержек); кратковременная деградация до 500 мс допускается только при пиковой нагрузке, соответствующей критерию нагрузочного теста (см. 7.1), и не является нормой для штатного режима работы.

Интуитивная навигация: запись на услугу -- не более 4 шагов (выбор услуги, мастера, времени и подтверждение записи).

Единая цветовая и типографическая система на основе Tailwind CSS, применяемая единообразно на всех страницах.

Поддержка тёмной и светлой темы -- опционально, не входит в MVP.

\subsection{Требования к функциям (задачам), выполняемым системой}
\label{sec:functions}
В таблицах 2--17 приведены детализации ключевых функций системы. Таблица~\ref{tab:roles} демонстрирует полноту покрытия функциями всех четырёх ролей: «клиент», «мастер», «администратор точки» и «владелец сети», включая функции ролей «мастер» и «администратор» (ФТ-011--ФТ-016).

\begin{xltabular}{\textwidth}{|X|c|c|c|c|}
\caption{Функции системы по ролям}\label{tab:roles}\\
\hline
\textbf{Функция} & \textbf{Клиент} & \textbf{Мастер} & \textbf{Админ.} & \textbf{Владелец} \\
\hline
\endfirsthead
\multicolumn{5}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Функция} & \textbf{Клиент} & \textbf{Мастер} & \textbf{Админ.} & \textbf{Владелец} \\
\hline
\endhead
\endfoot
Регистрация, вход, восстановление пароля (ФТ-001, ФТ-002, ФТ-003, ФТ-010) & + & + & + & + \\\hline
Просмотр слотов и запись (ФТ-004, ФТ-005) & + & -- & -- & -- \\\hline
Отмена/перенос своей записи (ФТ-006) & + & -- & -- & -- \\\hline
Просмотр своих записей, истории записей, выход из системы & + & + & + & + \\\hline
Просмотр каталога услуг & -- & -- & + & + \\\hline
Управление (создание/редактирование) каталога услуг (ФТ-007) & -- & -- & -- & + \\\hline
Получение писем-напоминаний о записи (ФТ-008) & + & + & -- & -- \\\hline
Редактирование контента сайта (ФТ-009) & -- & -- & -- & + \\\hline
Управление собственным расписанием (ФТ-011) & -- & + & + & + \\\hline
Подтверждение/\allowbreak завершение/\allowbreak отмена записи клиента (см. 4.2.1) & -- & + & + & + \\\hline
Назначение ролей, создание профиля мастера (ФТ-012) & -- & -- & + & + \\\hline
Отмена записи администратором (ФТ-013) & -- & -- & + & + \\\hline
Оставление отзыва (ФТ-014) & + & -- & -- & -- \\\hline
Модерация отзывов (ФТ-014) & -- & -- & + & + \\\hline
Аналитика загруженности мастеров, отчёты (ФТ-015) & -- & -- & + & + \\\hline
Управление точками сети (ФТ-016) & -- & -- & -- & + \\\hline
\end{xltabular}

Личный кабинет дополнительно обеспечивает функции, не требующие отдельной карточки ФТ ввиду тривиальности реализации: \textbf{выход из системы} (инвалидация текущей сессии -- удаление refresh-токена на стороне сервера и очистка cookie на клиенте) и \textbf{просмотр истории записей} (список записей текущего пользователя с фильтром по статусу, включая завершённые/отменённые, а не только текущие).

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Регистрация нового клиента (ФТ-001)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Регистрация нового клиента по email и паролю \\\hline
Идентификатор & ФТ-001 \\\hline
Цель и условие выполнения & Выполняется при обращении неаутентифицированного лица к форме регистрации на сайте барбершопа \\\hline
Входные данные & Email, пароль, подтверждение пароля, имя, фамилия, необязательный телефон \\\hline
Требования к обработке &
1) Email должен соответствовать формату адреса электронной почты, приводится к нижнему регистру и должен быть уникальным среди активных учётных записей. \\\cline{2-2}
 &
2) Пароль -- от 8 до 72 байт в UTF-8 (ограничение алгоритма хеширования bcrypt); требований к составу символов (регистр, цифры) нет. \\\cline{2-2}
 &
3) Телефон, если указан, -- от 5 до 20 символов, только цифры и необязательные \texttt{+ ( ) -} и пробелы; специфичного формата для российских номеров система не требует. \\\cline{2-2}
 &
4) Если email уже занят активной учётной записью -- ошибка 409 «Пользователь с таким email уже существует»; аналогично для телефона. \\\cline{2-2}
 &
5) При успешной валидации создаётся клиент, пароль сохраняется в виде хеша (bcrypt), клиенту сразу выдаются JWT-токены (клиент считается вошедшим в систему). Параллельно генерируется токен подтверждения email (срок действия 24 часа) и отправляется письмо со ссылкой -- подтверждение email не блокирует ни вход, ни какие-либо действия в системе (см. также ФТ-002). \\\cline{2-2}
 &
См. также ФТ-003 -- альтернативный способ регистрации/входа через VK ID. \\\hline
Выходные данные & JWT access- и refresh-токены (клиент сразу вошёл в систему); запись в таблице \texttt{users}; письмо на email со ссылкой подтверждения \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Подтверждение адреса электронной почты (ФТ-002)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Подтверждение email клиента; повторная отправка письма подтверждения \\\hline
Идентификатор & ФТ-002 \\\hline
Цель и условие выполнения & Подтверждение -- при переходе по ссылке из письма (действует и без входа в систему); повторная отправка -- по кнопке в баннере, доступной только уже вошедшему пользователю с неподтверждённым email \\\hline
Входные данные & Для подтверждения -- уникальный токен из ссылки; для повторной отправки -- входных данных не требуется, получатель определяется по текущей сессии \\\hline
Требования к обработке &
1) Проверка существования и срока действия токена (24 часа) при подтверждении; при недействительном или просроченном токене -- ошибка «Токен подтверждения email недействителен или просрочен», без указания вошёл ли пользователь в систему. \\\cline{2-2}
 &
2) При успешном подтверждении -- заполняется отметка времени подтверждения email, токен помечается использованным; новых токенов сессии подтверждение не выдаёт -- пользователь уже был или не был вошедшим независимо от этого действия. \\\cline{2-2}
 &
3) Повторная отправка письма для уже подтверждённого email -- ошибка 400 «Email уже подтверждён» (это отдельная проверка при нажатии «Отправить письмо ещё раз», а не результат перехода по использованной ссылке). \\\cline{2-2}
 &
4) Повторная отправка ограничена -- не более 3 запросов в час на пользователя, иначе ошибка 429 «Слишком много запросов. Попробуйте позже.»; каждая повторная отправка аннулирует ранее выданные, ещё не использованные токены этого пользователя. \\\hline
Выходные данные & Признак успешного подтверждения либо соответствующее сообщение об ошибке \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Авторизация через VK ID (OAuth 2.1, PKCE) (ФТ-003)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Авторизация клиента через сторонний OAuth-провайдер \\\hline
Идентификатор & ФТ-003 \\\hline
Цель и условие выполнения & Выполняется при нажатии клиентом кнопки «Войти через VK»; кнопка показывается, только если на сервере включена интеграция с VK ID \\\hline
Входные данные & Код авторизации (authorization code) и \texttt{code\_verifier}, возвращённые провайдером после согласия пользователя (обмен по протоколу OAuth 2.1 с PKCE, метод S256) \\\hline
Требования к обработке &
1) Система выполняет обмен кода на access-токен провайдера с проверкой \texttt{code\_verifier}. \\\cline{2-2}
 &
2) Получает профиль пользователя (идентификатор VK, имя, email при наличии). \\\cline{2-2}
 &
3) Ищет пользователя по идентификатору VK; если не найден -- ищет по email (если VK его вернул): если учётная запись с таким email уже существует (создана по паролю) -- идентификатор VK привязывается к ней, а email, если ещё не был подтверждён, считается подтверждённым; если нет -- создаётся новая учётная запись с ролью «клиент», без пароля. \\\cline{2-2}
 &
4) Проверка, что учётная запись не заблокирована -- иначе отказ. \\\cline{2-2}
 &
5) Выдаёт собственные JWT-токены (как при обычном входе). \\\cline{2-2}
 &
6) Ошибки провайдера (отказ пользователя, недействительный/просроченный код, сбой обмена токена) приводят к редиректу на страницу входа с кодом ошибки в адресе; интерфейс показывает пользователю соответствующее сообщение. \\\hline
Выходные данные & JWT access- и refresh-токены; учётная запись клиента (создана либо найдена/дополнена) \\\hline
Особенности & Email для клиентов, авторизованных через VK, может отсутствовать -- в этом случае поле email остаётся пустым, пока клиент не укажет его вручную (в профиле либо на шаге записи, см. ФТ-005), а функции, требующие email (в т.ч. запись к мастеру), до этого момента недоступны. \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Получение списка свободных временных слотов (ФТ-004)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Расчёт доступных для записи временных слотов \\\hline
Идентификатор & ФТ-004 \\\hline
Цель и условие выполнения & Выполняется при выборе клиентом мастера, услуги и даты на странице онлайн-записи \\\hline
Входные данные & \texttt{master\_id}, \texttt{service\_id}, \texttt{date} \\\hline
Требования к обработке &
1) Получить рабочее расписание мастера на день недели, соответствующий указанной дате; если для этого дня недели нет строки расписания либо день отмечен нерабочим -- вернуть пустой массив слотов. \\\cline{2-2}
 &
2) Получить длительность выбранной услуги. \\\cline{2-2}
 &
3) Пройти рабочее окно мастера (от начала до конца смены) шагом, равным длительности выбранной услуги; интервал, пересекающийся с уже существующей записью мастера, пропускается, и следующая попытка начинается сразу после окончания этой записи -- шаг не выровнен по фиксированной сетке. \\\cline{2-2}
 &
4) Слоты, целиком относящиеся к прошедшему времени (при запросе на сегодня), не возвращаются. \\\hline
Выходные данные & Массив доступных слотов в формате \{\texttt{start\_time}, \texttt{end\_time}\}; при отсутствии свободных слотов -- пустой массив \\\hline
Особенности & Слоты вычисляются динамически при каждом запросе, не хранятся отдельной сущностью. Технологического буфера между соседними записями одного мастера система не применяет -- следующая запись может начинаться сразу по окончании предыдущей. Клиентское приложение на шаге «Дата» предлагает выбор только из ближайших 14 дней -- это ограничение интерфейса, backend его не проверяет и рассчитает слоты на любую будущую дату, если запрос придёт в обход интерфейса. \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Создание записи (бронирование) (ФТ-005)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Создание записи клиента к мастеру \\\hline
Идентификатор & ФТ-005 \\\hline
Цель и условие выполнения & Выполняется вошедшим в систему клиентом после выбора мастера, услуги и слота; незарегистрированным посетителям и гостям функция недоступна -- запись без учётной записи система не создаёт \\\hline
Входные данные & \texttt{master\_id}, \texttt{service\_id}, \texttt{start\_time}; клиент определяется из токена сессии (\texttt{client\_id} отдельно не передаётся) \\\hline
Требования к обработке &
1) Проверка лимита: не более 5 одновременных активных (не завершённых и не отменённых) записей у клиента -- иначе ошибка 400. \\\cline{2-2}
 &
2) Проверка, что у клиента заполнен email -- иначе ошибка 400 «Укажите email в профиле, чтобы оформить запись» (актуально для клиентов, вошедших через VK ID без email, см. ФТ-003). \\\cline{2-2}
 &
3) Проверка существования и активности мастера и услуги, что мастер оказывает выбранную услугу, и что клиент не пытается записаться сам к себе (для случая, когда мастер бронирует у другого мастера). \\\cline{2-2}
 &
4) Проверка, что \texttt{start\_time} -- в будущем; иначе ошибка 400 «Нельзя записаться на прошедшее время». \\\cline{2-2}
 &
5) Расчёт \texttt{end\_time} = \texttt{start\_time} + длительность услуги; расчёт итоговой цены -- \texttt{price\_override} у пары «мастер -- услуга», если задан, иначе \texttt{цена услуги} $\times$ \texttt{коэффициент мастера} (округление до копеек). \\\cline{2-2}
 &
6) Проверка, что интервал [\texttt{start\_time}, \texttt{end\_time}) укладывается в рабочее расписание мастера на этот день недели и не выходит за часы работы точки -- иначе ошибка 400 с указанием причины (мастер не работает в этот день / запись выходит за рамки его рабочего времени). \\\cline{2-2}
 &
7) Предварительная проверка отсутствия пересечения с другими записями мастера, затем попытка \texttt{INSERT} в таблицу \texttt{appointments} с ограничением \texttt{EXCLUDE USING gist} по пересечению временного диапазона для одного мастера -- страхует от гонки двух одновременных запросов. \\\cline{2-2}
 &
8) При конфликте (нарушение EXCLUDE-ограничения) -- возврат ошибки 409 «Выбранное время уже занято». \\\cline{2-2}
 &
9) При успехе -- запись создаётся со статусом \texttt{pending} («ожидает подтверждения»); подтверждает её мастер или администратор вручную (см. 4.2.1). Отдельного письма-подтверждения о создании записи система не отправляет. \\\hline
Выходные данные & Объект созданной записи (\texttt{id}, статус \texttt{pending}, время, мастер, услуга, итоговая цена); либо сообщение об ошибке 400/404/409 \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Отмена записи клиентом (ФТ-006)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Отмена клиентом собственной записи \\\hline
Идентификатор & ФТ-006 \\\hline
Цель и условие выполнения & Выполняется клиентом из личного кабинета для собственной записи в статусе «ожидает подтверждения» или «подтверждена» \\\hline
Входные данные & \texttt{appointment\_id} \\\hline
Требования к обработке &
1) Проверка, что запись принадлежит обратившемуся клиенту -- иначе ошибка 403 «Можно отменить только свою запись». \\\cline{2-2}
 &
2) Проверка, что текущий статус записи допускает переход в «отменена» (\texttt{pending} или \texttt{confirmed}) -- иначе ошибка 400 с указанием текущего статуса. Ограничения по времени до визита нет -- клиент может отменить запись в любой момент вплоть до её начала, штрафов и дедлайна отмены система не применяет. \\\cline{2-2}
 &
3) Статус записи меняется на \texttt{cancelled}, слот освобождается. \\\cline{2-2}
 &
4) Онлайн-оплата в системе не предусмотрена (см. 3.2) -- механизм возврата денежных средств не требуется. \\\cline{2-2}
 &
5) Перенести время записи клиент самостоятельно не может -- эта операция доступна только мастеру или администратору (см. ФТ-013). \\\hline
Выходные данные & Обновлённый статус записи \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Управление каталогом услуг и индивидуальным прайсом мастера (ФТ-007)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Создание/редактирование услуги каталога и настройка её цены у мастеров \\\hline
Идентификатор & ФТ-007 \\\hline
Цель и условие выполнения & Создание, редактирование и скрытие услуги -- только владельцем сети (каталог -- общий актив всей сети); привязка услуги к мастеру и её индивидуальная цена у этого мастера -- владельцем сети либо администратором точки, к которой относится мастер \\\hline
Входные данные & Название услуги, описание (необязательно), базовая цена ($>$ 0), длительность в минутах ($>$ 0); для привязки к мастеру -- \texttt{master\_id}, необязательное точечное переопределение цены (\texttt{price\_override}) \\\hline
Требования к обработке &
1) Валидация: цена $>$ 0, длительность -- целое число минут $>$ 0 (кратность 15 минутам не требуется), название -- от 2 до 200 символов. \\\cline{2-2}
 &
2) Сохранение услуги в таблице \texttt{services}. \\\cline{2-2}
 &
3) Привязка услуги к мастеру -- таблица \texttt{master\_services}; итоговая цена услуги у мастера = \texttt{price\_override}, если он задан явно, иначе -- базовая цена услуги, умноженная на общий коэффициент цены мастера (\texttt{coefficient}, см. 5.1.1) и округлённая до копеек. \\\cline{2-2}
 &
4) Удаление привязки услуги к мастеру -- отдельным действием (без ограничений). Скрытие самой услуги из каталога (\texttt{is\_active = false}) -- мягкое, услугу можно вернуть в каталог повторным редактированием; физическое удаление услуги, на которую есть привязки к мастерам или записи, системой не предусмотрено. \\\hline
Выходные данные & Карточка услуги; записи в \texttt{master\_services}; обновлённый каталог, немедленно доступный через публичный API \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Автоматическая отправка писем-напоминаний о записи (ФТ-008)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Формирование и отправка клиенту письма-напоминания о предстоящей записи \\\hline
Идентификатор & ФТ-008 \\\hline
Цель и условие выполнения & Выполняется автоматически фоновым процессом внутри backend, опрашивающим базу данных каждые 5 минут (см. 4.1.1); отдельного воркера или очереди сообщений нет \\\hline
Входные данные & Записи в статусе «ожидает подтверждения» или «подтверждена», у которых до начала визита остался 24-часовой либо 2-часовой рубеж и соответствующее напоминание ещё не отправлено \\\hline
Требования к обработке &
1) На каждом проходе выбираются записи, которым пора получить напоминание «за 24 часа» (до визита от 2 до 24 часов, ещё не отправлено) и отдельно -- «за 2 часа» (до визита не более 2 часов, ещё не отправлено); если проход был пропущен (например, backend был недоступен) и запись уже попала в 2-часовое окно, ей отправляется только напоминание «за 2 часа», без дублирования. \\\cline{2-2}
 &
2) Формирование текста письма (тема, услуга, мастер, дата и время) и отправка на email клиента через SMTP. \\\cline{2-2}
 &
3) Ошибка сборки письма для одной записи не останавливает рассылку остальным в этом же проходе; ошибка отправки через SMTP не приводит к повторной попытке -- фиксируется в журнале сервера. \\\cline{2-2}
 &
4) После попытки отправки в поле \texttt{reminder\_24h\_sent\_at} либо \texttt{reminder\_2h\_sent\_at} записи проставляется метка времени -- она же служит признаком «уже отправлено» для следующих проходов. \\\hline
Выходные данные & Письмо клиенту на email; обновлённая метка времени отправки в записи \\\hline
Особенности & Уведомления мастеру, отдельные письма-подтверждения при создании записи и SMS-канал системой не реализованы (см. 3.3); мастер и администратор видят новые/изменённые записи непосредственно в личном кабинете/панели (4.2.1). \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Редактирование контента публичных страниц (главная, мастера) (ФТ-009)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Редактирование содержания и оформления сайта (тексты, цвета, шрифты, изображения, SEO) \\\hline
Идентификатор & ФТ-009 \\\hline
Цель и условие выполнения & Выполняется только владельцем сети в разделе «Настройки» -- редактирование выполняется прямо на самой главной странице сайта, отдельной формы нет \\\hline
Входные данные & Тексты блоков главной страницы, выбор цветовой темы и пары шрифтов, файлы изображений (логотип, hero-фото), заголовок вкладки браузера, описание для поисковых систем, favicon \\\hline
Требования к обработке &
1) Валидация текстовых полей на длину. \\\cline{2-2}
 &
2) Загрузка изображений: допустимые форматы JPEG/PNG/WebP/GIF, ограничение по размеру файла -- не более 5 МБ; проверка типа выполняется по заголовку Content-Type файла, содержимое (в т.ч. разрешение) не анализируется. \\\cline{2-2}
 &
3) Замена изображения сохраняет новый файл под новым именем и обновляет ссылку на него в настройках; предыдущий файл с диска не удаляется. \\\cline{2-2}
 &
4) Обновление единственной строки конфигурации (таблица \texttt{site\_settings}, содержимое хранится в поле \texttt{content} в формате JSON). \\\hline
Выходные данные & Обновлённые настройки, немедленно отражающиеся на публичных страницах \\\hline
Особенности & Адрес и часы работы на публичном сайте (подвал, блок «Наши салоны») эта функция не редактирует -- они берутся из данных точек сети (см. ФТ-016). \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Восстановление пароля (ФТ-010)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Восстановление доступа к учётной записи по забытому паролю \\\hline
Идентификатор & ФТ-010 \\\hline
Цель и условие выполнения & Выполняется при обращении к форме «Забыли пароль?» неаутентифицированным пользователем; для учётной записи, созданной только через VK ID (без пароля), функция бессмысленна -- восстанавливать нечего \\\hline
Требования к обработке &
1) По указанному email ищется учётная запись; независимо от результата поиска клиенту показывается одно и то же сообщение (не раскрывает, зарегистрирован ли адрес). \\\cline{2-2}
 &
2) При наличии учётной записи -- генерируется одноразовый токен сброса (срок действия 30 минут), отправляется письмо со ссылкой; лимит -- не более 3 запросов в час на адрес. \\\cline{2-2}
 &
3) По переходу на форму нового пароля с валидным токеном -- установка нового \texttt{password\_hash} (валидация как в ФТ-001), все ранее выданные JWT-токены пользователя аннулируются (принудительный выход со всех устройств), токен сброса аннулируется. \\\hline
Выходные данные & Письмо со ссылкой сброса; после установки нового пароля -- сообщение об успехе и предложение войти \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Управление расписанием мастера (ФТ-011)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Ведение недельного рабочего графика мастера \\\hline
Идентификатор & ФТ-011 \\\hline
Цель и условие выполнения & Мастер редактирует собственное расписание; администратор точки/владелец сети -- расписание мастеров своей точки (владелец -- любой точки) \\\hline
Входные данные & \texttt{master\_id}, день недели (0 -- понедельник, ..., 6 -- воскресенье), \texttt{start\_time}, \texttt{end\_time}, признак \texttt{is\_working} \\\hline
Требования к обработке &
1) Расписание -- одна строка на каждый день недели (не более семи на мастера), без привязки к конкретной календарной дате; отпуска, больничные и разовые переносы смены отдельными типами дня система не выражает -- на такие дни день недели временно переводится в \texttt{is\_working = false} вручную. \\\cline{2-2}
 &
2) При \texttt{is\_working = true} -- валидация \texttt{start\_time} $<$ \texttt{end\_time}, а также попадания смены в часы работы точки, к которой относится мастер -- иначе ошибка «Расписание должно быть в пределах времени работы салона (ЧЧ:ММ--ЧЧ:ММ)». \\\cline{2-2}
 &
3) При \texttt{is\_working = false} -- поля \texttt{start\_time}/\texttt{end\_time} не проверяются, день недели полностью недоступен для записи (ФТ-004 вернёт пустой список слотов). \\\cline{2-2}
 &
4) Изменение расписания не проверяется на конфликт с уже существующими записями мастера на этот день недели -- система не отменяет их и не предупреждает администратора; если после сужения смены запись выходит за её пределы, ответственность за перенос/отмену такой записи -- на мастере/администраторе (ФТ-013). \\\hline
Выходные данные & Обновлённая строка расписания на выбранный день недели \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Назначение ролей и создание профиля мастера (ФТ-012)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Изменение роли пользователя и связанное управление профилем мастера \\\hline
Идентификатор & ФТ-012 \\\hline
Цель и условие выполнения & Выполняется администратором точки (роли \texttt{client}/\texttt{master} и мастера только своей точки) либо владельцем сети (без ограничений) в разделе «Пользователи» \\\hline
Входные данные & \texttt{user\_id}, новая роль (\texttt{client}/\texttt{master}/\texttt{admin}); при назначении \texttt{admin} -- точка, которой будет управлять пользователь; при создании профиля мастера -- точка (для владельца сети обязательна, для администратора точки подставляется автоматически) \\\hline
Требования к обработке &
1) Проверка, что запрашивающий имеет роль \texttt{admin}/\texttt{owner}; назначение роли \texttt{admin} доступно только \texttt{owner} (роль \texttt{owner} через смену роли не назначается и не снимается). \\\cline{2-2}
 &
2) Назначение роли \texttt{admin} требует, чтобы пользователю уже была назначена точка (см. отдельное действие «привязка точки») -- иначе ошибка 400 «Сначала назначьте пользователю точку». \\\cline{2-2}
 &
3) Смена роли на \texttt{master} сама по себе профиль мастера не создаёт -- это отдельное действие «Создать профиль мастера», доступное после смены роли; если профиль ранее существовал и был деактивирован -- он реактивируется вместо создания нового, чтобы не терять историю записей и отзывов; попытка создать профиль повторно активному мастеру -- ошибка 409 «Профиль мастера уже существует». \\\cline{2-2}
 &
4) При смене роли на любую, кроме \texttt{master}, -- существующий профиль мастера деактивируется (\texttt{is\_active = false}), не удаляется. \\\hline
Выходные данные & Обновлённый пользователь; профиль мастера (создан/реактивирован/деактивирован) \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Управление статусом и переносом записи мастером/администратором (ФТ-013)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Подтверждение, завершение, отмена и перенос времени записи со стороны мастера или администратора \\\hline
Идентификатор & ФТ-013 \\\hline
Цель и условие выполнения & Выполняется мастером для своих записей либо администратором/владельцем сети без этого ограничения; в отличие от ФТ-006 (отмена клиентом), доступны также подтверждение, завершение и перенос времени \\\hline
Входные данные & \texttt{appointment\_id}; для смены статуса -- новый статус (\texttt{confirmed}/\texttt{done}/\texttt{cancelled}); для переноса -- новый \texttt{start\_time} \\\hline
Требования к обработке &
1) Проверка, что мастер меняет статус/переносит только свою запись -- иначе 403; администратор/владелец -- без этого ограничения. \\\cline{2-2}
 &
2) Переход статуса проверяется по фиксированной схеме: \texttt{pending} $\to$ \texttt{confirmed} $\to$ \texttt{done}; \texttt{cancelled} достижим из \texttt{pending} или \texttt{confirmed}; \texttt{done} и \texttt{cancelled} -- терминальные, дальнейших переходов из них нет. Переход, не входящий в допустимую схему, -- ошибка 400. \\\cline{2-2}
 &
3) Перенос времени допустим только для записи в статусе \texttt{pending} или \texttt{confirmed}, только на будущее время, только у того же мастера и той же услуги -- смена мастера или услуги требует отмены и повторного бронирования (ФТ-005); новое время проверяется теми же правилами, что и при создании (в пределах рабочего расписания мастера, отсутствие пересечения с другими записями). \\\cline{2-2}
 &
4) Ни привязки к моменту начала визита, ни ограничения по времени до визита ни у одной из операций нет -- в т.ч. отметить запись завершённой можно до фактического начала визита. \\\cline{2-2}
 &
5) Онлайн-оплата в системе не предусмотрена (см. 3.2) -- механизм возврата денежных средств не требуется. \\\cline{2-2}
 &
6) При переносе времени клиенту отправляется письмо с прежним и новым временем записи (см. ФТ-008); при подтверждении/завершении/отмене отдельного уведомления клиенту не отправляется. \\\hline
Выходные данные & Обновлённый статус записи либо обновлённое время записи \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Оставление и модерация отзыва (ФТ-014)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Отзыв клиента о завершённой записи; публикация/скрытие отзыва администратором \\\hline
Идентификатор & ФТ-014 \\\hline
Цель и условие выполнения & Клиент оставляет отзыв после визита; администратор точки модерирует отзывы только о мастерах своей точки, владелец сети -- обо всех мастерах сети \\\hline
Входные данные & \texttt{appointment\_id}, оценка \texttt{rating} (1--5), комментарий (опционально, до 2000 символов); для модерации -- \texttt{review\_id}, признак \texttt{is\_published} \\\hline
Требования к обработке &
1) Проверка, что запись в статусе \texttt{done} и её клиент совпадает с текущим пользователем. \\\cline{2-2}
 &
2) Проверка, что отзыв на эту запись ещё не оставлен (уникальность \texttt{appointment\_id} в \texttt{reviews}); при повторной попытке -- ошибка 409. \\\cline{2-2}
 &
3) Текст комментария автоматически проверяется на нецензурную лексику и наличие ссылок/адресов сайтов -- при обнаружении запрос отклоняется с соответствующим сообщением, отзыв не сохраняется. \\\cline{2-2}
 &
4) Отзыв публикуется сразу (\texttt{is\_published = true} по умолчанию), без предварительной модерации; средний рейтинг мастера не хранится отдельно, а вычисляется по запросу как среднее по всем опубликованным отзывам мастера. \\\cline{2-2}
 &
5) Администратор/владелец может установить \texttt{is\_published = false} для уже опубликованного отзыва -- отзыв не удаляется, но не показывается на публичной странице мастера и не учитывается при расчёте рейтинга; повторная публикация возможна тем же действием в обратную сторону. \\\hline
Выходные данные & Созданный отзыв \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Формирование отчёта по выручке и загруженности мастеров (ФТ-015)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Отчёт по выручке, записям и загруженности мастеров за период \\\hline
Идентификатор & ФТ-015 \\\hline
Цель и условие выполнения & Выполняется администратором точки (только своя точка) или владельцем сети (своя точка либо вся сеть на выбор) в разделе «Отчёты» \\\hline
Входные данные & \texttt{date\_from}, \texttt{date\_to}; точка -- неявно из роли администратора либо выбором владельца сети \\\hline
Требования к обработке &
1) За период вычисляются сводные показатели: общая выручка, общее число записей, средний чек, доля клиентов с более чем одной записью за период («повторные клиенты»). \\\cline{2-2}
 &
2) Строится график выручки по дням периода и разбивка числа записей по услугам. \\\cline{2-2}
 &
3) Для каждого мастера точки (сети) вычисляется: число записей, выручка, средний чек, средняя оценка по опубликованным отзывам. \\\cline{2-2}
 &
4) По кнопке «Экспорт Excel» тот же отчёт формируется файлом .xlsx с несколькими листами (сводка, выручка по дням, детализация по мастерам). \\\hline
Выходные данные & Объект отчёта с перечисленными показателями и разбивками, пригодный для отображения на странице; либо файл .xlsx \\\hline
\end{xltabular}

\begin{xltabular}{\textwidth}{|P|D|}
\caption{Управление точками сети (ФТ-016)}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Параметр} & \textbf{Описание} \\
\hline
\endhead
\endfoot
Наименование функции & Создание, редактирование и закрытие/открытие точки сети \\\hline
Идентификатор & ФТ-016 \\\hline
Цель и условие выполнения & Выполняется только владельцем сети в разделе «Точки сети» \\\hline
Входные данные & Название, адрес, необязательный телефон, время открытия и закрытия, необязательное фото; для закрытия/открытия -- признак \texttt{is\_active} \\\hline
Требования к обработке &
1) Валидация: время закрытия позже времени открытия -- иначе ошибка «close\_time должен быть позже open\_time». \\\cline{2-2}
 &
2) При создании точки короткое имя для URL (\texttt{slug}) генерируется сервером из названия автоматически, вручную не редактируется. \\\cline{2-2}
 &
3) Точка физически не удаляется -- закрытие переводит её в \texttt{is\_active = false}: точка пропадает с публичного сайта, но её мастера, записи и отчёты остаются доступны; открытие возвращает \texttt{is\_active = true}. \\\cline{2-2}
 &
4) Часы работы точки -- жёсткая граница: расписание мастеров этой точки (ФТ-011) и время записи клиента (ФТ-004, ФТ-005) не могут выходить за эти рамки; сужение часов работы уже назначенные смены мастеров и существующие записи не корректирует и не отменяет автоматически. \\\hline
Выходные данные & Карточка точки; обновлённый список точек, используемый фильтром каталога мастеров и переключателем точки в панели администратора \\\hline
\end{xltabular}


\section{Требования к видам обеспечения}
\label{sec:provision}
\subsection{Информационное обеспечение}

\subsubsection{Состав основных сущностей}

Модель построена вокруг точки сети (\texttt{salons}): пользователь, мастер и запись ссылаются на конкретную точку, что обеспечивает разграничение видимости данных администратора его точкой (см. 3.1, 4.1.4). Профиль мастера (\texttt{masters}) выделен в отдельную сущность и отделён от учётной записи пользователя (\texttt{users}) связью 1:0..1, поскольку не каждый пользователь является мастером. Помимо основных, бизнес-ориентированных сущностей, в модель включены служебные сущности, обеспечивающие безопасность: журнал попыток входа, токены сброса пароля и подтверждения email, сессии входа.

Правило генерации идентификаторов: все первичные ключи -- \texttt{UUID v4}, генерируются на стороне приложения при создании записи (не автоинкремент -- исключает угадывание/перечисление идентификаторов через API).

Основные сущности представлены в таблице~\ref{tab:entities}.

\begin{xltabular}{\textwidth}{|p{4.2cm}|X|}
\caption{Основные сущности системы и их атрибуты}\label{tab:entities}\\
\hline
\textbf{Сущность} & \textbf{Основные атрибуты} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Сущность} & \textbf{Основные атрибуты} \\
\hline
\endhead
\endfoot
\texttt{salons} (Точка сети) & \texttt{id}, \texttt{name}, \texttt{slug}, \texttt{address}, \texttt{phone}, \texttt{open\_time}, \texttt{close\_time}, \texttt{photo\_url}, \texttt{is\_active}, \texttt{created\_at} \\\hline
\texttt{users} (Пользователь) & \texttt{id}, \texttt{first\_name}, \texttt{last\_name}, \texttt{phone}, \texttt{email}, \texttt{password\_hash}, \texttt{vk\_user\_id}, \texttt{email\_verified\_at}, \texttt{role}, \texttt{salon\_id} (FK, только для роли «администратор»), \texttt{is\_blocked}, \texttt{token\_version}, \texttt{deleted\_at}, \texttt{created\_at} \\\hline
\texttt{masters} (Мастер) & \texttt{id}, \texttt{user\_id} (FK $\to$ users), \texttt{salon\_id} (FK $\to$ salons), \texttt{specialization}, \texttt{photo\_url}, \texttt{coefficient} -- множитель цены услуг мастера, \texttt{is\_active}, \texttt{deleted\_at}, \texttt{created\_at} \\\hline
\texttt{services} (Услуга) & \texttt{id}, \texttt{name}, \texttt{description}, \texttt{price}, \texttt{duration\_min}, \texttt{is\_active}, \texttt{deleted\_at} \\\hline
\texttt{master\_services} (Услуга мастера) & \texttt{master\_id} (FK), \texttt{service\_id} (FK), \texttt{price\_override} -- необязательное точечное переопределение цены (если не задано, цена = \texttt{services.price} $\times$ \texttt{masters.coefficient}) \\\hline
\texttt{schedules} (Расписание мастера) & \texttt{id}, \texttt{master\_id} (FK), \texttt{day\_of\_week} (0--6), \texttt{start\_time}, \texttt{end\_time}, \texttt{is\_working}, \texttt{created\_at} \\\hline
\texttt{appointments} (Запись) & \texttt{id}, \texttt{client\_id} (FK $\to$ users), \texttt{master\_id} (FK), \texttt{service\_id} (FK), \texttt{salon\_id} (FK), \texttt{start\_time}, \texttt{end\_time}, \texttt{final\_price}, \texttt{status}, \texttt{reminder\_24h\_sent\_at}, \texttt{reminder\_2h\_sent\_at}, \texttt{created\_at} \\\hline
\texttt{reviews} (Отзыв) & \texttt{id}, \texttt{appointment\_id} (FK, уникальный), \texttt{client\_id} (FK), \texttt{master\_id} (FK), \texttt{service\_id} (FK), \texttt{rating}, \texttt{comment}, \texttt{is\_published}, \texttt{created\_at} \\\hline
\texttt{login\_attempts} (Попытка входа) & \texttt{id}, \texttt{email\_attempted}, \texttt{user\_id} (FK, может быть пуст), \texttt{ip\_address}, \texttt{success}, \texttt{created\_at} \\\hline
\texttt{password\_reset\_tokens} (Токен сброса пароля) & \texttt{id}, \texttt{user\_id} (FK), \texttt{token\_hash}, \texttt{expires\_at}, \texttt{used\_at}, \texttt{created\_at} \\\hline
\texttt{email\_verification\_tokens} (Токен подтверждения email) & \texttt{id}, \texttt{user\_id} (FK), \texttt{token\_hash}, \texttt{expires\_at}, \texttt{used\_at}, \texttt{created\_at} \\\hline
\texttt{sessions} (Сессия входа) & \texttt{id}, \texttt{user\_id} (FK), \texttt{token\_hash}, \texttt{expires\_at}, \texttt{created\_at} \\\hline
\texttt{site\_settings} (Настройки сайта) & \texttt{id}, \texttt{content} (JSON: тексты, цветовая тема, шрифты, SEO), \texttt{created\_at} \\\hline
\end{xltabular}

Во всех таблицах, хранящих токены (сброс пароля, подтверждение email, сессия), в базе данных сохраняется только SHA-256-хеш токена -- сам токен существует только в ссылке/cookie на стороне пользователя и не восстановим по содержимому базы данных.

\subsubsection{Модель организации данных}

Инфологическая модель разделена на две схемы для читаемости: рисунок~1 -- сущности бизнес-домена (точки, пользователи, мастера, услуги, записи, отзывы); рисунок~2 -- сущности, обеспечивающие безопасность входа, и настройки сайта. Точка сети (\texttt{salons}) -- корневая сущность сети: на неё ссылаются мастер, администратор (через \texttt{users.salon\_id}) и запись; профиль мастера отделён от учётной записи пользователя связью 1:0..1 (не каждый пользователь -- мастер); услуга мастера (\texttt{master\_services}) -- связь M:N между мастером и услугой с необязательным переопределением цены.

\begin{sidewaysfigure}[htbp]
\centering
\includegraphics[height=\textwidth, width=\textheight, keepaspectratio]{images/ER_final_1.png}
\caption{Инфологическая модель -- бизнес-домен}
\end{sidewaysfigure}

\begin{sidewaysfigure}[htbp]
\centering
\includegraphics[height=\textwidth, width=\textheight, keepaspectratio]{images/ER_final_2.png}
\caption{Инфологическая модель -- аутентификация и настройки сайта}
\end{sidewaysfigure}

% \clearpage выталкивает оба повёрнутых рисунка до начала следующего пункта:
% иначе они всплывают уже после его текста и отрываются от пункта, к которому
% относятся (рисунок помещается сразу после ссылки на него).
\clearpage

Настройки сайта (пунктирная рамка на рисунке~2) -- служебная сущность без связей с остальной моделью: это единственная строка конфигурации, не имеющая внешних ключей ни в одну другую таблицу и ни одной ссылающейся на неё.

Правила ссылочной целостности: прикладной код никогда не выполняет физическое (\texttt{DELETE}) удаление строк \texttt{users}/\texttt{masters}/\texttt{services} -- вместо этого используется мягкое удаление (поле \texttt{deleted\_at}) или деактивация (\texttt{is\_active = false}), история операций сохраняется. Ограничения внешних ключей на уровне БД -- защитный рубеж на случай прямого изменения данных в обход приложения: удаление \texttt{users}/\texttt{masters}/\texttt{services}, на которые ссылаются \texttt{appointments}, запрещено (\texttt{ON DELETE RESTRICT}); зависимые записи \texttt{masters} (\texttt{schedules}, \texttt{master\_services}), \texttt{users} (\texttt{sessions}, токены, отзывы) и \texttt{appointments} (\texttt{reviews}) удаляются каскадно (\texttt{ON DELETE CASCADE}) только вместе со своим родителем.

\subsubsection{Состав входной и выходной информации}

Обязательность полей примеров ниже указана отдельным перечнем перед каждым примером.

Запись клиента к мастеру (все поля обязательны; клиент определяется из токена сессии, см. ФТ-005):
\begin{verbatim}
{
  "master_id": "b3f1...",
  "service_id": "a7c2...",
  "start_time": "2026-07-05T14:00:00+03:00"
}
\end{verbatim}

Услуга каталога (обязательные поля: \texttt{name}, \texttt{price}, \texttt{duration\_min}; \texttt{description} -- опционально):
\begin{verbatim}
{
  "name": "Стрижка + борода",
  "description": "Классическая стрижка с оформлением бороды",
  "price": 2500,
  "duration_min": 60
}
\end{verbatim}

Привязка услуги к мастеру, с необязательным переопределением цены (обязательно только \texttt{service\_id}):
\begin{verbatim}
{
  "service_id": "a7c2...",
  "price_override": 3000
}
\end{verbatim}

Выходная информация. Созданная запись:
\begin{verbatim}
{
  "id": "152e...",
  "status": "pending",
  "master": {"id": "b3f1...", "first_name": "Алексей", "last_name": "П."},
  "service": {"id": "a7c2...", "name": "Стрижка + борода", "price": 2500},
  "start_time": "2026-07-05T14:00:00+03:00",
  "end_time": "2026-07-05T15:00:00+03:00",
  "final_price": 3000
}
\end{verbatim}

Отчёт по выручке за период (для администратора точки/владельца сети):
\begin{verbatim}
{
  "date_from": "2026-06-01",
  "date_to": "2026-06-30",
  "total_revenue": 385000.00,
  "total_appointments": 210,
  "avg_check": 1833.33,
  "repeat_clients_pct": 34.5,
  "revenue_by_day": [
    {"date": "2026-06-01", "revenue": 12500.00}
  ],
  "appointments_by_service": [
    {"service_name": "Стрижка + борода", "appointments": 72}
  ],
  "masters_breakdown": [
    {
      "master_name": "Алексей П.",
      "appointments": 58,
      "revenue": 145000.00,
      "avg_check": 2500.00,
      "avg_rating": 4.8
    }
  ]
}
\end{verbatim}

\subsubsection{Экранные формы}

Экранные формы представлены на рисунках А.1--А.6 в приложении А.

\subsubsection{Классификаторы и кодовые системы}

Классификатор и его кодовая система представлены в таблице~\ref{tab:status-classifier}. Отдельного классификатора категорий услуг система не ведёт -- у услуги нет поля категории, каталог не группируется программно (см. 5.1.1). Каналов доставки писем в системе только один (email, см. 3.3 и ФТ-008), классификатор для одного значения не требуется.

\begin{xltabular}{\textwidth}{|l|l|X|}
\caption{Классификатор статусов записи}\label{tab:status-classifier}\\
\hline
\textbf{Код} & \textbf{Название} & \textbf{Описание} \\
\hline
\endfirsthead
\multicolumn{3}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Код} & \textbf{Название} & \textbf{Описание} \\
\hline
\endhead
\endfoot
pending & Ожидает подтверждения & Запись создана, слот зарезервирован, ждёт подтверждения мастером/администратором \\\hline
confirmed & Подтверждена & Мастер или администратор подтвердил запись \\\hline
done & Завершена & Мастер отметил запись выполненной \\\hline
cancelled & Отменена & Отменена клиентом, мастером или администратором \\\hline
\end{xltabular}

\subsection{Лингвистическое обеспечение}

Язык интерфейса: русский.

Основные термины: «Запись», «Слот», «Мастер», «Услуга», «Клиент», «Личный кабинет», «Напоминание».

Сокращения в пользовательском интерфейсе не допускаются; в документации допускаются общепринятые технические сокращения (API, JWT, ORM).

\subsection{Программное обеспечение}

\begin{xltabular}{\textwidth}{|l|X|}
\caption{Программное обеспечение}\\
\hline
\textbf{Компонент} & \textbf{Технология} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Компонент} & \textbf{Технология} \\
\hline
\endhead
\endfoot
Backend & Python 3.14, FastAPI, SQLAlchemy 2.0 (синхронный), Pydantic v2, Alembic \\\hline
Frontend & Vue.js 3 (Composition API), Pinia, Tailwind CSS, Vite \\\hline
База данных & PostgreSQL 16 \\\hline
Напоминания о записи & фоновый asyncio-цикл в процессе backend, без выделенного воркера (см. 4.1.1) \\\hline
Аутентификация & JWT (python-jose), OAuth 2.1 с PKCE (VK ID) \\\hline
Контейнеризация & Docker, docker-compose \\\hline
Веб-сервер & Caddy (внешний обратный прокси: приём трафика на портах 80/443, TLS-терминация, автоматический сертификат Let's Encrypt); Nginx -- внутри контейнера \texttt{frontend}: раздача собранного веб-приложения и внутреннее проксирование запросов \texttt{/api/*} на backend \\\hline
Веб-аналитика & Яндекс.Метрика (счётчик подключён и активен, вынесен в отдельный файл \texttt{/metrika.js}) \\\hline
\end{xltabular}

\subsection{Техническое обеспечение}

\begin{xltabular}{\textwidth}{|l|X|}
\caption{Необходимое техническое обеспечение}\\
\hline
\textbf{Ресурс} & \textbf{Требование} \\
\hline
\endfirsthead
\multicolumn{2}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{Ресурс} & \textbf{Требование} \\
\hline
\endhead
\endfoot
Сервер & VPS: от 2 CPU, 4 GB RAM, 40 GB SSD (обоснование ёмкости относительно нагрузочного теста -- см. раздел 7.1) \\\hline
Клиент & Браузер с поддержкой ES2020+: Chrome 90+, Firefox 85+, Safari 14+ \\\hline
Сеть & Постоянное интернет-соединение для клиента и сервера \\\hline
Хранилище & Объектное хранилище или локальный диск для медиафайлов (логотип, фото мастеров, изображения услуг) -- от 5 GB \\\hline
\end{xltabular}


% ============================================================
\section{Состав и содержание работ по созданию системы}
% ============================================================

Сумма длительностей этапов приведена в соответствие с датами начала и окончания работ (раздел 1.3) -- 27 дней (22.06--18.07.2026). Этап «Интеграции» включает настройку фонового планировщика напоминаний (см. также раздел 5.3). Этапы «Тестирование» и «Развёртывание и сдача» объединены в один этап.

\begin{xltabular}{\textwidth}{|c|P|D|>{\centering\arraybackslash}p{2.4cm}|}
\caption{Состав и содержание работ}\\
\hline
\textbf{№} & \textbf{Этап} & \textbf{Содержание} & \textbf{Срок} \\
\hline
\endfirsthead
\multicolumn{4}{l}{Продолжение таблицы \arabic{table}}\\
\hline
\textbf{№} & \textbf{Этап} & \textbf{Содержание} & \textbf{Срок} \\
\hline
\endhead
\endfoot
1 & Проектирование & Обследование бизнес-процессов, проектирование схемы БД, диаграммы прецедентов & 22.06--25.06 (4 дня) \\\hline
2 & Разработка backend API & Реализация моделей, роутеров, авторизации (JWT, VK ID OAuth), логики слотов & 26.06--03.07 (8 дней) \\\hline
3 & Разработка frontend & Публичная часть (главная, запись, личный кабинет), административная панель & 04.07--10.07 (7 дней) \\\hline
4 & Интеграции & Настройка отправки email (SMTP), фонового планировщика напоминаний, интеграции с VK ID & 11.07--14.07 (4 дня) \\\hline
5 & Тестирование, развёртывание и сдача & Функциональное и нагрузочное тестирование, исправление ошибок, деплой на сервер, подготовка документации, демонстрация комиссии & 15.07--18.07 (4 дня) \\\hline
\end{xltabular}

% ============================================================
\section{Порядок контроля и приёмки системы}
% ============================================================

\subsection{Виды, состав и методы испытаний}

\textbf{Функциональное тестирование:}
\begin{itemize}
  \item проверка всех функций ФТ-001--ФТ-016;
  \item тестирование граничных условий: одновременная запись двух клиентов на один слот (проверка EXCLUDE-ограничения); попытка отменить уже завершённую или ранее отменённую запись (проверка машины состояний, 4.2.1); попытка регистрации с уже существующим email;
  \item проверка корректности расчёта индивидуальных цен по мастерам.
\end{itemize}

\textbf{Тестирование интерфейса:}
\begin{itemize}
  \item кроссбраузерное тестирование (Chrome, Firefox, Safari);
  \item проверка адаптивности на мобильных разрешениях (375--414 px).
\end{itemize}

\textbf{Нагрузочное тестирование:}
\begin{itemize}
  \item одновременная работа не менее 20 пользователей с формой онлайн-записи без деградации времени отклика свыше 500 мс;
  \item проверка устойчивости EXCLUDE-ограничения при конкурентных запросах на один и тот же слот.
\end{itemize}

Обоснование технических требований к серверу (раздел 5.4) относительно нагрузочного теста: 20 одновременных пользователей формы записи создают, по оценке, не более 40 запросов/с к API в пике диалога выбора слота; при среднем времени ответа типового эндпоинта FastAPI менее 50 мс конфигурации 2 vCPU / 4 GB RAM достаточно с запасом для процесса backend и PostgreSQL -- отдельного воркера, для которого требовался бы дополнительный запас, в системе нет (см. 4.1.1). Показатель оценочный и уточняется по фактическим результатам нагрузочного тестирования.

\textbf{Критерии приёмки.} Система считается прошедшей приёмочные испытания при одновременном выполнении условий:
\begin{enumerate}
  \item все функции ФТ-001--ФТ-016 работают в соответствии с настоящим ТЗ;
  \item не выявлено ни одного критического дефекта -- делающего невозможным выполнение сценариев регистрации/входа, онлайн-записи или административного управления записями;
  \item число некритических дефектов не превышает 5 на каждые 100 проверенных тест-кейсов;
  \item нагрузочное тестирование пройдено без деградации времени отклика свыше 500 мс (см. выше).
\end{enumerate}

\textbf{Порядок повторного тестирования.} При обнаружении критического дефекта приёмочные испытания в затронутой области приостанавливаются; разработчик устраняет дефект в срок не более 2 рабочих дней; после устранения проводится регрессионное повторное тестирование только затронутого функционала (соответствующих тест-кейсов из раздела 7.1), а не полного объёма испытаний заново. Полный повторный прогон требуется только при изменениях, затрагивающих более 30\% проверенных сценариев.

\subsection{Порядок сдачи этапов работ}

\begin{itemize}
  \item еженедельные демонстрации функциональности научному руководителю;
  \item промежуточные отчёты по завершении каждого этапа (раздел 6);
  \item финальная демонстрация системы в полном объёме;
  \item защита проекта перед комиссией.
\end{itemize}

% ============================================================
\section{Требования к документированию}
% ============================================================

\subsection{Перечень документов}

\begin{itemize}
  \item техническое задание (данный документ);
  \item отчёт об обследовании бизнес-процессов (диаграммы прецедентов, физическая диаграмма, диаграммы действий, таблицы операций и документов);
  \item описание архитектуры системы и ER-диаграмма базы данных;
  \item программа и методика испытаний (ГОСТ РД 50-34.698-90) -- определяет объём, порядок и критерии испытаний, детализируя раздел 7.1 настоящего ТЗ;
  \item документация API (OpenAPI/Swagger, автогенерируемая средствами FastAPI);
  \item руководство пользователя (для клиента и администратора);
  \item руководство администратора по развёртыванию (Docker, переменные окружения, миграции Alembic);
  \item отчёт о тестировании;
  \item исходный код с комментариями (репозиторий);
  \item презентация проекта для защиты.
\end{itemize}

Документ подготовлен на основе результатов исследования бизнес-процессов барбершопов и парикмахерских и охватывает требования, необходимые для проектирования и реализации системы.

\clearpage
\appendix
% Единственное приложение -- буква "А" зафиксирована явно, без счётчика
% приложений; счётчик рисунков сброшен, чтобы рисунки приложения шли
% как А.1, А.2, ... (ГОСТ), а не продолжали сквозную нумерацию рисунка 1.
\renewcommand{\thefigure}{А.\arabic{figure}}
\setcounter{figure}{0}
\gostappendix{А}{Экранные формы}

Рисунки приложения пронумерованы последовательно А.1--А.6 и представляют собой макеты экранных форм (wireframes), а не снимки экрана работающей системы.

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Barbers_2.png}}
  \caption{Каталог мастеров (публичная часть сайта)}
\end{figure}

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Main_4.png}}
  \caption{Главная страница (публичная часть сайта)}
\end{figure}

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Services_and_prices_6.png}}
  \caption{Услуги и цены (публичная часть сайта)}
\end{figure}

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Dashboard_1.png}}
  \caption{Панель администратора -- раздел «Дашборд» (статистика)}
\end{figure}

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Reports_5.png}}
  \caption{Панель администратора -- раздел «Отчёты»}
\end{figure}

\begin{figure}[htbp]
  \centering
  \fbox{\includegraphics[width=0.9\textwidth,height=0.8\textheight,keepaspectratio]{images/Gallery_3.png}}
  \caption{Галерея (публичная часть сайта)}
\end{figure}

\clearpage
\renewcommand{\thefigure}{Б.\arabic{figure}}
\setcounter{figure}{0}
\gostappendix{Б}{Диаграммы последовательностей}

Диаграммы Б.1--Б.5 иллюстрируют порядок взаимодействия участников для ключевых бизнес-процессов системы, детализированных в карточках функциональных требований (раздел 4.2).

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgB{-0.1}{4.5}{Заполнение формы регистрации\\(email, пароль, имя...)}
  \seqmsgA{4.3}{9.0}{POST /api/auth/register}
  \seqmark{\cTop}
  \seqmsgB{9.0}{13.4}{Проверка уникальности\\email/телефона}
  \seqmark{\dTopA}
  \seqretA{13.4}{9.0}{Результат проверки}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{email или телефон уже заняты}
  \seqretB{9.0}{4.3}{409 Пользователь с таким\\email уже существует}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{email и телефон свободны}
  \seqselfA{9.0}{Хеширование пароля}
  \seqmsgA{9.0}{13.4}{INSERT в users}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{Пользователь создан}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqselfA{9.0}{Генерация JWT\\access/refresh}
  \seqselfB{9.0}{Токен подтверждения email,\\отправка письма}
  \seqretA{9.0}{4.3}{201 Created + JWT-токены}
  \seqmark{\cBot}
  \seqact{9.0}{\cTop}{\cBot}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}

  \seqmsgB{4.5}{-0.1}{Переход в личный кабинет\\(клиент уже вошёл)}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Регистрация» (ФТ-001)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgA{-0.1}{4.5}{Ввод email и пароля}
  \seqmsgA{4.3}{9.0}{POST /api/auth/login}
  \seqmark{\cTop}
  \seqmsgB{9.0}{13.4}{Поиск пользователя\\по email}
  \seqmark{\dTopA}
  \seqretB{13.4}{9.0}{Данные пользователя\\(password\_hash) либо не найден}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{польз. не найден /\\ вход временно заблокирован}
  \seqretA{9.0}{4.3}{401 Неверный email или пароль}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{пользователь найден,\\блокировки нет}
  \seqselfA{9.0}{Проверка пароля (bcrypt)}

  \seqpad{0.35}\seqmark{\altBTop}
  \seqcond{7.0}{пароль неверный}
  \seqmsgA{9.0}{13.5}{Запись неудачной попытки входа}
  \seqretA{9.0}{4.3}{401 Неверный пароль}
  \seqpad{0.3}\seqmark{\sepB}
  \seqpad{0.3}
  \seqcond{7.0}{пароль верный}
  \seqselfB{9.0}{Генерация JWT access\\+ refresh-сессии}
  \seqmsgA{9.0}{13.4}{Сохранение сессии\\(хеш refresh-токена)}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{OK}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretB{9.0}{4.3}{200 + access-токен;\\refresh в httpOnly cookie}
  \seqmark{\cBot}
  \seqpad{0.35}\seqmark{\altBBot}
  \seqframe{alt}{0.4}{13.6}{\altBTop}{\altBBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}

  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTop}{\cBot}

  \seqmsgA{4.5}{-0.1}{Переход в личный кабинет}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Вход по email и паролю»}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgA{-0.1}{4.5}{Выбор мастера, услуги и даты}
  \seqmsgB{4.3}{9.0}{GET /api/slots?master\_id\\\&service\_id\&date}
  \seqmark{\cTopA}
  \seqmsgB{9.0}{13.4}{Запрос расписания\\и записей мастера}
  \seqmark{\dTopA}
  \seqretA{13.4}{9.0}{Расписание + список записей}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}
    \seqselfB{9.0}{Расчёт свободных слотов\\шаг = длительность услуги}
  \seqretA{9.0}{4.3}{Список свободных слотов}
  \seqmark{\cBotA}
  \seqact{9.0}{\cTopA}{\cBotA}

  \seqmsgA{4.5}{-0.1}{Отображение слотов}
  \seqmsgB{-0.1}{4.5}{Выбор слота,\\подтверждение записи}
  \seqmsgA{4.3}{9.0}{POST /api/appointments}
  \seqmark{\cTopB}
  \seqselfB{9.0}{Проверки: лимит, email,\\расписание, будущее время}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{нарушена проверка /\\ конфликт слота}
  \seqretA{9.0}{4.3}{400/409 ошибка}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{все проверки пройдены}
  \seqmsgB{9.0}{13.4}{INSERT в appointments\\(EXCLUDE USING gist)}
  \seqmark{\dTopB}
  \seqretB{13.4}{9.0}{Запись создана\\(status=pending)}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretA{9.0}{4.3}{201 Created + данные записи}
  \seqmark{\cBotB}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTopB}{\cBotB}

  \seqmsgB{4.5}{-0.1}{«Запись создана,\\ожидает подтверждения»}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Онлайн-запись на приём» (ФТ-004, ФТ-005)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Клиент}
  \seqhead{4.4}{Клиентское\\приложение}
  \seqhead{9.0}{Сервер\\(API)}
  \seqhead{13.4}{База\\данных}

  \seqmsgB{-0.1}{4.5}{Нажатие «Отменить запись»\\в личном кабинете}
  \seqmsgA{4.3}{9.0}{POST /api/appointments/\{id\}/cancel}
  \seqmark{\cTop}
  \seqmsgA{9.0}{13.4}{Поиск записи по id}
  \seqmark{\dTopA}
  \seqretB{13.4}{9.0}{Данные записи\\(client\_id, статус)}
  \seqmark{\dBotA}
  \seqact{13.4}{\dTopA}{\dBotA}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{6.7}{запись принадлежит\\другому клиенту}
  \seqretB{9.0}{4.3}{Можно отменить только\\свою запись}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{6.7}{статус не допускает отмену}
  \seqretA{9.0}{4.3}{400 ошибка с текущим статусом}
  \seqpad{0.3}\seqmark{\sepB}
  \seqpad{0.3}
  \seqcond{6.7}{запись своя,\\статус допускает отмену}
  \seqmsgA{9.0}{13.4}{UPDATE status = cancelled}
  \seqmark{\dTopB}
  \seqretA{13.4}{9.0}{Успех}
  \seqmark{\dBotB}
  \seqact{13.4}{\dTopB}{\dBotB}
  \seqretA{9.0}{4.3}{200 + обновлённый статус}
  \seqmark{\cBot}
  \seqpad{0.35}\seqmark{\altBot}
  \seqaltframe{-0.6}{13.8}{\altTop}{\altBot}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqsep{-0.6}{\sepA}{8.9}
  \seqsep{9.1}{\sepA}{13.8}
  \seqact{9.0}{\cTop}{\cBot}

  \seqmsgA{4.5}{-0.1}{«Запись отменена»}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Клиент}{\finalY}
  \seqfoot{4.4}{Клиентское\\приложение}{\finalY}
  \seqfoot{9.0}{Сервер\\(API)}{\finalY}
  \seqfoot{13.4}{База\\данных}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Отмена записи клиентом» (ФТ-006)}
\end{figure}

\begin{figure}[htbp]
\centering
\resizebox{1\textwidth}{!}{%
\begin{tikzpicture}
  \seqinit
  \seqhead{0}{Планировщик}
  \seqhead{4.6}{База данных}
  \seqhead{9.2}{Email-сервис}

  \seqselfB{-0.1}{Пробуждение по таймеру\\(каждые 5 минут)}
  \seqmsgB{-0.1}{4.6}{Выборка записей с рубежом\\24ч/2ч и неотправл. напоминанием}
  \seqmark{\bTop}
  \seqretA{4.6}{-0.1}{Список записей}
  \seqmark{\bBot}
  \seqact{4.6}{\bTop}{\bBot}

  \seqpad{0.35}\seqmark{\loopTop}
  \seqcond{2.3}{для каждой записи в списке}
  \seqmark{\aTop}
  \seqpad{0.75}\seqmark{\seqy}\draw[seqmsg] ($(0,\seqy)!0.09cm!(9.2,\seqy)$) -- ($(9.2,\seqy)!0.09cm!(0,\seqy)$) node[pos=0.75, above] {Отправка письма-напоминания};
  \seqmark{\cTop}
  \seqpad{0.75}\seqmark{\seqy}\draw[seqret] ($(9.2,\seqy)!0.09cm!(0,\seqy)$) -- ($(0,\seqy)!0.09cm!(9.2,\seqy)$) node[pos=0.25, above] {Статус отправки};
  \seqmark{\cBot}
  \seqact{9.2}{\cTop}{\cBot}

  \seqpad{0.35}\seqmark{\altTop}
  \seqcond{2.3}{успешно}
  \seqmsgA{0}{4.7}{UPDATE reminder\_24h/2h\_sent\_at}
  \seqpad{0.3}\seqmark{\sepA}
  \seqpad{0.3}
  \seqcond{2.3}{ошибка отправки}
  \seqselfB{0}{Логирование ошибки;\\повтор на следующем цикле}
  \seqpad{0.35}\seqmark{\altBot}
  \seqframe{alt}{-0.7}{9.3}{\altTop}{\altBot}
  \seqsep{0.1}{\sepA}{9.3}

  \seqmark{\aBot}
  \seqact{0}{\aTop}{\aBot}
  \seqpad{0.35}\seqmark{\loopBot}
  \seqloopframe{-1}{9.6}{\loopTop}{\loopBot}

  \seqpad{0.9}\seqmark{\finalY}
  \seqfoot{0}{Планировщик}{\finalY}
  \seqfoot{4.6}{База данных}{\finalY}
  \seqfoot{9.2}{Email-сервис}{\finalY}
\end{tikzpicture}}
\caption{Диаграмма последовательностей бизнес-процесса «Автоматическая отправка напоминаний» (ФТ-008)}
\end{figure}

\end{document}
