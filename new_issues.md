1. [x] Продумать систему отзывов (клиент может оставлять отзыв к каждому приему, или 1 отзыв = 1 аккаунт?). — решено: оставляем как есть (1 отзыв на 1 запись, уже так и реализовано).
2. [x] Все токены пользователей сохраняются как хэш в БД. Пользователь вышел из системы -> удаляются все токены из БД. (Изменить продолжительность жизни токенов). — построена полная сессионная система: новая таблица `sessions` (SHA-256 хеш refresh-токена, expires_at), access-токен укорочен до 15 мин (`access_token_expire_minutes`), refresh живёт 30 дней (`refresh_token_expire_days`, отдельная httpOnly-cookie `refresh_token` на путь `/api/auth`). `POST /api/auth/refresh` обновляет access с ротацией refresh-токена (старая сессия удаляется, выдаётся новая). Logout удаляет ровно текущую сессию (одно устройство), а не все разом — как было раньше через `token_version`. События безопасности (смена/сброс пароля, блокировка, мягкое удаление) по-прежнему бампают `token_version` (мгновенный отзыв access) и теперь дополнительно удаляют все сессии пользователя (`session_repo.delete_all_for_user`) — полный логаут со всех устройств. Фронтенд (`api/client.js`) на 401 сначала тихо пробует refresh и повторяет запрос, и только если не удалось — разлогинивает. Проверено вживую (миграция + register/refresh-ротация/logout/повторное использование токена → 401) на одноразовой Postgres, плюс юнит-тесты (test_auth_unit.py, test_admin_session_revocation.py).
3. [x] HTTP Response в 201 статусе -> все ок, так можно. — подтверждено, изменений не требуется.
4. [x] String should match pattern '^\+?[0-9()\- ]{5,20}$' - не должно быть при вводе номера телефона. — 422-ошибки теперь переводятся в понятный русский текст (см. utils/errors.js friendlyValidationError), сырой regex больше не показывается.
5. [x] Исправить полное обновление страницы при редактировании/добавлении/неправильном вводе данных. — двум формам (AdminSettings, AdminMasters «добавить услугу») не хватало атрибута `novalidate`; без него нативная HTML5-валидация браузера перехватывала submit раньше, чем срабатывал `@submit.prevent`, и это выглядело как «зависание»/скролл-джамп на невалидных обязательных полях.
6. [x] Исправить округление средней продолжительности стрижки до 10 минут. — hero-плашка на главной теперь округляет к ближайшим 10 минутам (было — к ближайшей минуте).
7. [x] Исправить запись (прямоугольники второго ряда больше), записаться на вчерашний день не должно быть возможно — кнопки дат теперь фиксированной ширины (w-20), проверено визуально (Playwright): все 14 дат в двух рядах одинакового размера. Запись на прошедший день и так была невозможна (дата-пикер строится от "сегодня" вперёд, бэкенд отдельно отклоняет start_time в прошлом) — код не менялся, только подтверждено.
8. [x] Исправить отображение описания услуги. — description услуги нигде не отображался клиенту (только в админке); добавлено на страницу записи (BookingPage) под названием услуги при выборе.
9. [x] Коэффициент поменять — убрана сортировка "по коэффициенту" на публичной /masters, вместо неё реальная сортировка "по цене" (final price с учётом override).
10. [x] https://saitama.monster/api/appointments/my?page=1&page_size=10 - недостаточно прав — мастер заходил на /profile, эндпоинт клиентский; секция "Мои записи" теперь скрыта для роли master.
11. [x] У барберов не показывать фамилию на карточках — MasterCard.vue показывает только first_name.
12. [x] Номер телефона не должно быть обязательным полем в личном кабинете /profile (без указывания номера телефона мастер не может поменять фамилию) — пустая строка теперь отправляется как null (PhoneStr | None не принимает '').
13. [x] Пользователь может записываться на бесконечное количество слотов? — добавлен лимит (5 одновременных активных записей, settings.max_active_appointments_per_client).
14. [x] Реализовать уведомления за 24 и 2 часа — email-напоминания (единственный готовый канал доставки в проекте). Две метки времени на appointments (reminder_24h_sent_at/reminder_2h_sent_at) вместо отдельной таблицы уведомлений (та уже была и её снесли неиспользуемой — см. 0005_drop_notifications). Опрашивается asyncio-лупом в процессе бэкенда каждые settings.reminder_poll_interval_seconds (5 мин по умолчанию, без внешней очереди задач/APScheduler — работает с одним экземпляром бэкенда). Проверено вживую (реальная отправка на SMTP-заглушку, идемпотентность при повторных опросах, "запись перенеслась в 2-часовое окно, пока не отправили 24ч" не даёт задвоения, отменённые записи пропускаются) плюс юнит-тесты (test_reminder_service.py).
15. [x] Добавить цензуру на сайте — фильтр на Review.comment (единственное место, где текст от недоверенного пользователя публикуется сразу, без модерации). Отклоняется на входе схемы (`ReviewCreate`, 422 с понятным русским текстом — фронтенд уже умеет показывать `value_error` как есть, ничего менять не пришлось). Свой список корней нецензурной лексики (`utils/content_filter.py`, RU+EN) вместо стороннего пакета (для русского мата готовых Python-библиотек нет) — проверяется как префикс каждого отдельного слова после нормализации (разделители, повтор букв, латиница-двойники кириллицы), не подряд по всему тексту, чтобы не путать обычные слова ("плохую", "художник", "хутор") с матом. Заодно закрыт п. 29а — тот же валидатор запрещает ссылки/домены. Проверено вживую через реальный API (профанность/ссылка → 422 с читаемым текстом, чистый отзыв → публикуется как раньше) плюс юнит-тесты (test_content_filter.py, test_review_schema.py).
16. [x] Добавить возможность мастеру вручную переносить запись — новый `PATCH /api/appointments/{id}/reschedule` (мастер-владелец или админ), та же валидация, что и при создании (рабочие часы, отсутствие пересечений через `get_overlapping(..., exclude_id=...)`), только для pending/confirmed. Сбрасывает `reminder_24h_sent_at`/`reminder_2h_sent_at` (иначе перенесённая запись могла остаться без напоминаний на новое время), клиенту уходит email об изменении времени (переиспользует SMTP-инфраструктуру напоминаний). `/masters/{id}/slots` получил `exclude_appointment_id`, чтобы собственный текущий слот записи не считался "занятым" при выборе нового времени. На фронтенде — кнопка "Перенести" в DashboardAppointments + модалка `RescheduleModal.vue` (тот же UX выбора даты/времени, что и на BookingPage). Проверено вживую (реальный API: exclude_appointment_id освобождает свой слот, перенос меняет БД и шлёт письмо, отказ на прошедшее время/пересечение/чужую запись) плюс юнит-тесты (test_appointment_reschedule.py, test_slots.py).
17. [x] checkboxes should be fixes everywhere — плагина `@tailwindcss/forms` не было, поэтому классы стилизации на нативных чекбоксах не давали эффекта (рендерились как обычные браузерные, вразнобой по размеру); добавлен компонент `BaseCheckbox.vue` и применён во всех 3 местах использования.
18. [x] При создании мастера -> профиля мастера, и последующей выдачи роли клиента, профиль мастера остается в бд, и необходимо перерегистрировать аккаунт заново. — теперь повторное назначение роли master реактивирует старый профиль вместо блокирующего 409.
19. [x] Пофиксить чекбоксы, отображение времени должно быть в 24 часовом формате — чекбоксы см. #17. ИСПРАВЛЕНИЕ: изначально решили проблему проп'ом `lang="ru"` на `BaseInput.vue`, но при живой проверке (headless-браузер с `locale: 'en-US'`) выяснилось, что `lang` на элементе НЕ управляет форматом нативного `<input type="time">` в Chromium — виджет всё равно показывал "09:00 AM". Настоящий фикс — см. #35 (BaseTimeInput.vue).
[x] Парикмахеры не должны работать в нерабочее время. — уже реализовано: `AppointmentService._validate_within_schedule` проверяет расписание мастера (`is_working`/`start_time`/`end_time` по дню недели) перед созданием записи и в `get_available_slots`; изменений не потребовалось.
20. [x] Все ошибки у клиентов и мастеров отображались "дружелюбно, по-русски" без регулярок, названий переменных. — тот же фикс, что #4: friendlyValidationError переводит item.type/msg в русский текст.
21. [x] Мои записи всегда отображается как актив таб — SidebarLink.vue: active-class -> exact-active-class (index-пункт с path:'' подсвечивался на любом вложенном маршруте, например /dashboard/schedule).
22. [x] Календарь при выборе дат должен быть русский с русскими дд/мм/гггг — ИСПРАВЛЕНИЕ: та же ошибка, что и в #19 — `lang="ru"` не заставляет нативный `<input type="date">` показывать дд/мм/гггг, при проверке всё ещё показывал "07/01/2026" (US-формат). Настоящий фикс — см. #38 (BaseDateInput.vue). Кастомный список дат на BookingPage уже был на русском (текстовые месяцы), не менялся.
23. [x] За "Мастера" при обращении к записи выдаются все данные -> fetch /api/appointments/{id} -> выдаются все данные — client в ответе сужен до имени+телефона (ClientBriefResponse) вместо полного UserResponse.
24. [x] /masters -> при сортировке по услугам после выбора любой услуги нельзя выбрать "любую услугу" — BaseSelect.vue: placeholder-option был disabled (навсегда невыбираем повторно), убрано.
25. [x] Как указывать специализацию мастера? — решено: оставляем свободным текстом, без изменений.
26. [x] Отмененный записи клиентов/пользователей как долго будут висеть в личном кабинете (сделать 1 месяц?) или пусть всегда будут в бд пока аккаунт существует — решено: хранятся всегда, пока существует аккаунт, без изменений.
27. [x] При soft delete что должно сохраняться в бд? — решено: текущее поведение верное (deleted_at + RESTRICT на FK), без изменений.
28. [x] /api/setup остаётся технически достижимым (не 404) даже после завершения настройки - защищён токеном, но можно было бы полностью закрывать после первого запуска для порядка — POST /api/setup теперь отдаёт 404 вместо 409 после завершения настройки.
29. [x] Запретить возможность указывать любые домены/ссылки/etc. — уточнено: (а) запретить ссылки/домены в отзывах/комментариях, видимых другим пользователям — сделано вместе с #15 (тот же валидатор `ReviewCreate.comment`, `contains_link` в `utils/content_filter.py`); (б) добавить загрузку файлов для главного изображения и фото мастеров вместо произвольного URL — новый `POST /api/admin/uploads/image` (multipart, только admin, белый список типов JPEG/PNG/WebP/GIF, лимит 5 МБ, серверное имя файла) сохраняет на диск и раздаёт через `/api/uploads/...` (StaticFiles); `ImageUpload.vue` заменил текстовые поля URL в AdminSettings (hero) и AdminMasters (фото мастера). Проверено вживую: загрузка, отдача файла обратно побайтово, отказ без авторизации и на неподдерживаемый тип.
30. [x] Изменить дизайн кнопки сортировки на мобильных девайсах — на /masters и в AdminUsers кнопка-переключатель сортировки была отдельной колонкой грида без mobile-варианта и растягивалась на всю ширину как одинокая полоса с крошечной иконкой; теперь она в одном ряду с select "сортировка по" (flex, w-11 shrink-0).
31. [x] Проверить .lower() для адресов электронных почт (адрес не должен быть case sensitive). — новый NormalizedEmailStr (schemas/fields.py) применён везде, где email принимается от пользователя (регистрация/логин/сброс пароля/админ-редактирование/VK ID).
32. [x] Экспериментальная страница — редактирование прямо на реальной главной странице. Обычные «Настройки» не затронуты. — убрать и полность проверить соответствие данной страницы реально отображающемуся контенту на сайте. — уточнено: не убирать страницу, убрать только предупреждающую пометку "экспериментальная"/"(тест)". — убран amber-баннер в AdminSettingsLive.vue и лейбл "Настройки (тест)" в сайдбаре/заголовке (AdminPanel.vue) заменён на "Редактор главной" с иконкой PencilSquareIcon вместо BeakerIcon (у которой была лабораторная коннотация).
33. [x] Мастер не должен иметь возможность записаться сам к себе. — уже было реализовано ранее; добавлен регрессионный тест.
34. [x] Изменить отображение кнопок (div class="mb-4 flex flex-wrap items-center gap-3") на мобильных устройствах — в DashboardAppointments фильтры (статус + 2 даты) имели фиксированную ширину (w-48/w-44) и криво переносились на узких экранах; статус теперь на всю ширину, даты — по 50% в ряд на мобильных, фиксированная ширина остаётся с sm: и выше.

35. [x] При составлении расписания мастера переделать часовой формат в 24ч часовой формат. — подтверждено вживую (headless-браузер, `locale: 'en-US'`): нативный `<input type="time">` игнорирует `lang` на элементе и показывал "09:00 AM". Заменён на `BaseTimeInput.vue` — два `<select>` (часы 00–23, минуты 00–59), гарантированно 24-часовой формат независимо от локали браузера/ОС. Применён в DashboardSchedule.vue и AdminMasters.vue (расписание). Проверено и визуально, и функционально (реальное сохранение 10:30 в БД через новый пикер).
36. [x] При учете, что парикмахерская закрывается в 20:00, я могу записаться к мастеру на услугу, продолжительностью 90 минут, на 19:30. Нужно сделать отдельную настройку в настройках, в которой бы явно через админ-панель указывалось бы время работы парикмахерской. И учитывать эти ограничения при составлении расписания. — новое поле `business_hours` (open_time/close_time, по умолчанию 09:00–20:00) в site_settings.content, редактируется в AdminSettings (новая секция «Время работы салона», BaseTimeInput). Не путать с footer.hours — тот просто текст. Проверено в двух точках: (1) MasterService.set_schedule/update_schedule — расписание мастера не может выйти за эти рамки; (2) AppointmentService._validate_within_schedule — бэкстоп на уровне самой записи (на случай расписаний мастера, заведённых раньше). Живой прогон воспроизвёл ровно баг из формулировки (90 мин в 19:30 при закрытии в 20:00 → 400) и подтвердил, что валидные времена по-прежнему проходят.
37. [x] Добавить Яндекс.Метрику (счётчик 110627746, с webvisor) — сниппет вставлен в `frontend/index.html` (script в `<head>`, noscript-пиксель сразу после открытия `<body>`, как рекомендует Яндекс). Уточнено и подтверждено отдельно: webvisor:true — это запись сессий (клики/движения), не только счётчик просмотров, на сайте есть формы с именами/телефонами/email — пользователь подтвердил, что хочет именно такой конфиг.

38. [x] В отчетах при выборе даты календать сделать на русском языке. — тот же баг, что и #22 (native input[type=date] игнорирует lang), подтверждено скриншотом с `locale: 'en-US'` ("07/01/2026"). Заменено на `BaseDateInput.vue` (день/месяц/год, три select, месяцы на русском — "1 июл 2026") в AdminReports.vue и DashboardAppointments.vue. Проверено функционально: выбор 5/март/2026 корректно ушёл в API как `date_from=2026-03-05`.
39. [x] Можно изменить внешний вид input-bub7g4s / select-ynvv82e и подобных кнопок на что-то более соответствующее стилистике сайта — уточнено (на примере /admin/users): это `<select>` из `BaseSelect.vue` — без `appearance-none` браузер рисовал нативный виджет (голый, без border-radius/шрифта сайта) поверх заданных классов. Добавлены `appearance-none` + свой `ChevronDownIcon` вместо системной стрелки — теперь select визуально не отличается от инпутов/кнопок сайта. Эффект глобальный (BaseSelect используется везде), проверено скриншотами на /admin/users и /masters (десктоп и мобильная раскладка).

40. При выборе мастера, должно ли в ссылке отображаться его id?

41. [ ] TODO: завести аккаунт на sentry.io и вписать SENTRY_DSN/VITE_SENTRY_DSN в .env — регистрация на sentry.io была недоступна (403) на момент подключения мониторинга ошибок. Код (sentry_sdk.init в backend/app/main.py, Sentry.init в frontend/src/main.js) готов и ждёт DSN — без него просто не инициализируется. Подробности и порядок действий — README «Мониторинг ошибок (Sentry)».
